{
  "rules": {
    ".read": false,
    ".write": false,
    "workflow": {
      ".read": true,
      ".write": true
    },
    "authorizedUsers": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": false
      }
    },
    "accessRequests": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid && auth.token.email_verified === true && newData.child('email').val() === auth.token.email",
        ".validate": "newData.hasChildren(['email', 'displayName', 'requestedAt', 'lastSeenAt'])",
        "email": {
          ".validate": "newData.isString() && newData.val() === auth.token.email"
        },
        "displayName": {
          ".validate": "newData.isString()"
        },
        "requestedAt": {
          ".validate": "newData.isNumber()"
        },
        "lastSeenAt": {
          ".validate": "newData.isNumber()"
        },
        "$other": {
          ".validate": false
        }
      }
    },
    "caseSettings": {
      ".read": "auth != null && auth.token.email_verified === true && root.child('authorizedUsers').child(auth.uid).child('caseAccess').val() === true && root.child('authorizedUsers').child(auth.uid).child('email').val() === auth.token.email",
      ".write": "auth != null && auth.token.email_verified === true && root.child('authorizedUsers').child(auth.uid).child('caseAccess').val() === true && root.child('authorizedUsers').child(auth.uid).child('email').val() === auth.token.email"
    },
    "cases": {
      ".read": "auth != null && auth.token.email_verified === true && root.child('authorizedUsers').child(auth.uid).child('caseAccess').val() === true && root.child('authorizedUsers').child(auth.uid).child('email').val() === auth.token.email",
      ".write": "auth != null && root.child('authorizedUsers').child(auth.uid).child('email').val() === auth.token.email && ((auth.token.email_verified === true && root.child('authorizedUsers').child(auth.uid).child('caseAccess').val() === true) || root.child('authorizedUsers').child(auth.uid).child('writerAccess').val() === true)"
    }
  }
}
