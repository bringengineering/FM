# CSV shared ImportJob audit

Continue original ImportJob scope. Complete company sharing requires schema, trusted Main/server publication, append-only permissions, read UI/report/export/backup handling, emulator/native tests and company/two-PC acceptance. No deployment allowed in this task.

- [x] RED schema tests: exact immutable local ImportJob, fresh verified company Drive original, creator binding, original draft-only scope, collision policy/dry-run/rollback/source metadata, checksum and size restrictions.
- [x] csv-import-audit.js builds/validates canonical shared audit metadata using existing local job validator and Drive reference validator. No raw CSV binary or automatic visit writes. This validator is consistency evidence, never authenticated origin/authorization.
- [x] Main audit preparation: job/file IDs only, fresh approved actor and current shared project, authoritative Drive reverification, owned metadata/time and summary-only IPC with canPublish=false. No DB posting or binary source return.
- [x] Server append-only transaction compiler/updater: private compiled handles, validated audit binding and account-owned source metadata; preserve all root data, fresh grants/project on each retry, exact receipt replay and conflict refusal. Authentication/Drive validator caller must remain trusted, not request data.
- [x] CSV publication authentication: verified callable identity plus current Admin SDK account/revocation time and matching member/admin CRM/RND approvals; typed auth/permission errors, SDK outages separate. Bind current role from storage, never role claims. Endpoint must pass request.auth only.
- [x] Portable CSV server runtime: build-owned CommonJS package preserves rnd-control/core relative paths; copy exact authoritative dependencies with source SHA inventory; cloud verifies shipped package without desktop source; adapter exposes bounded audit validators/digest and fixed-company Drive verification. No UI or tokens in audit output.
- [x] Trusted server preparation foundation: authenticate owner twice, verify/download bounded Drive bytes, reparse exact source drafts with authoritative external revisit parents, enforce row classification/secret checks, server time/project binding and private compile. No DB write; tests include rehashed forgery and external revisit.
- [ ] Trusted publication operation with fresh current CRM/RND access and authoritative Drive verification; atomic append/idempotent same-job replay; reject changed job and stale project; handle uncertain writes separately. Extend restore snapshot/runtime compatibility without rewriting old history.
- [ ] Append-only database permissions and emulator tests for two users, approvals, malformed data and collisions. Keep operational rules undeployed.
- [ ] Main/preload explicit publish, team read-only history, reports and backup preservation. Native and packaged verification.
- [ ] Company real account Drive original, separate approved reviewer/PC and full acceptance proof. Preserve original 61 gates.
