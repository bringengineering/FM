# Shared restore preflight
Full target: approved immutable history/shared restoration. This prerequisite is read-only, not completion of shared restore.
Require fresh admin RND access before and after current project/visit reads and validation. Verified metadata backup only. Hydrate current project maps; validate links and frozen CRM contexts for current/incoming. Report exact preserve-existing/new IDs, source/current revisions, hashes, protected research/CRM counts and visit collision/binding details. canApply=false until dedicated approval/CAS/Rules route is implemented. No write dependency.
TDD service rejects member before reads, changed/revoked identity after async read, unverified/tampered backup, invalidcontexts; preserves all inputs and reports conflicts+historical counts. Main IPC/preload read-only integration with localfixture lists; native/UI approval route follows in later work.

Approval preparation: bind reviewed and freshly re-read admin preview by canonical actor/source/project/visit hashes; preserve source immutable histories; admit only new IDs. New visits referring to colliding existing project/original visit require separate linkage review. The plan is server-side preparatory code only, not IPC exposed and canApply=false. Next: atomic root snapshot, approved restore audit/CAS transaction and compatible Firebase Rules, then native UI and company environment verification.

Atomic approval preparation now reads rndControl root once with X-Firebase-ETag and fresh admin/session checks. Both collection comparisons derive from that same cloned snapshot and bind snapshot ETag/content SHA in atomicPlanSHA256. No commit route/IPC is exposed yet. Next implement conditional root commit with an immutable approval audit and Rules emulator permission tests; do not use PATCH as ETag CAS.

Integration limitation confirmed in review: current rndControl root .read=false, although child collections permit reads. Therefore this preparatory repository root read receives403 under current real Rules. Next must add narrowly scoped approved-admin root read and emulator tests before IPC exposure; mocked repository tests do not prove production access.

Admin root read Rules implemented locally: requires enabled CRM admin with token-email match plus enabled matching-email/role R&D approval. Root writes remain false. Actual demo database emulator passed root allow/ordinary roles/CRM-only/anonymous/wrong email/R&D revoke/CRM revoke and restored permission tests, plus full existing immutable/revision suite. The previous root403 integration limitation is resolved in development Rules only; not deployed. Node438 passed. Evidence rnd-restore-root-rules-results.txt. CAS commit/approved restore audit Rules remain next.

Mutation compiler added (server-side pure helper only): recomputes source and atomic plan hashes and snapshot hash, rejects target collisions, preserves complete original root/unknown fields, adds only reviewed new IDs with fresh revision1 and trusted operation actor/time. One restoreApprovals audit retains source revision/hash and approval reason. Metadata histories remain unchanged. No IPC/write route is exposed; checksums do not authenticate approval. Next: conditional PUT If-Match, response-loss recovery, emulator Rules for approval audit and historical restore authorization, UI and package update.

Conditional commit transport added as unexposed development helper: compiles the trusted prepared plan using Main-generated UUID/time, checks fresh approved admin before request and after response, sends one full-root PUT with If-Match, rejects412 without retry, verifies acknowledgement content and recovers uncertain response through exactly one full-root GET matching operation audit/full content. No auto write retry. Current Rules have no approval-audit/historical restore write authorization, so actual restore commit remains denied until next Rules tests/implementation. Transport mocked tests are not Firebase CAS integration proof.
