# CSV Baseline Import Implementation Plan

> Continue the approved original CRM/R&D scope. User authorized autonomous recommended implementation; no repeated design approval. This bounded step builds the parser/preview/provenance foundation; UI, durable import job registration, rollback manifest and operational acceptance remain required next.

Requirements: original planning sections8/ImportJob/CRM-04/UX-04/OPS-04. Existing baseline minute/cost/revisit/project validation stays authoritative. UTF-8 CSV files are read without modifying original bytes. Source hash/size/name/ledger/extraction time/column mapping and fixed KEEP_EXISTING duplicate policy belong to preview. Explicit minutes=min and costs=KRW declarations are required; no implicit unit conversion. Blank numeric values are errors; literal0 valid. No automatic measurements approval or CRM write. Explicit delimiter comma/semicolon/tab, strict quoted cells and escaped quotes, CRLF and multiline fields. Bound8MiB/10000 data and blank records/128columns/32768chars percell. Duplicate/empty headers, ragged rows, unterminated quotes, invalidUTF8 and NUL rejected. Mapping uses header names; each source column at most once; unknown mapping fields rejected. Missing sourceID gets deterministic source/project/record identity; mapped IDs must be valid and unique.

Files: csv-import.mjs browser-safe parser and normalized validated preview; rnd-csv-import.test.js meaningful fixture/error/collision/links/sourcehash tests. Source chronology must be strict ISO; sourcebytes hash includes BOM and CRLF. Preview row errors disable all application; existing IDs stay unchanged; revisit relationships checked against existing+new rows. Cell content is never executed. Raw source remains outside preview payload except used normalized fields; column mapping/provenance does not authenticate source truth.

Steps: write RED tests; implement parser; implement mapped normalization+preview through validateVisit/previewVisitImport; test malformed/missing0/collision/revisit/literalformula/hash/encoding/limits; scoped review; fulldesktop tests; record partial gate evidence without marking wholeCRM-04 PASS; commit/push/sourcepackage refresh. Then complete native Main/preload/UI and source-preserving rollback/import manifest separately, followed by packaged and company operational tests.
