# Local draft restore manifest
Requirement CRM-04 rollback manifest: preserve originals and record restoration before/after. Current authorized restore only adds local project drafts; no company cloud deletion/replacement. Full CSV/approved immutable-history restoration remains separate.
Design: new restore-manifest.mjs creates versioned metadata-only manifest with id/time/selected-before, full before/incoming snapshots, exact computed preview and expected-after projectSHA256 inventory, manifest integritySHA256. Creator clones inputs before awaiting digest. Reader checks whitelist/scope/version/hash/allprojects/selected/preview/recomputed after inventory. Rollback function returns before snapshots only if complete current project inventory and hashes exactly equal expected-after, rejecting any later additions/edits/saves. Checksums detect corruption, not authentication.
UI: create manifest after approval, recheck epoch/project/title-input/cache after asynchronous hashing, apply drafts and retain downloadable manifest. Reset clears cached manifest/button. No shared DB writes. Browser download request is not proof of disk or Drive persistence.
TDD: new module absent fails; then roundtrip/collision original preservation, tampered hash/plan/inventory, empty before and rollback refusal after edits/additions. Native download capture verifies bytes; delayed/reset safeguards required. Review, fullNode/native, commit. Packaged/delivery verification remains next step. Rollback UI not yet exposed.

Checkpoint: missing-module test failed before implementation. Roundtrip/tamper/exact-after rollback and source-clone tests pass. Recomputed-checksum malformed ID test exposed weak ID pattern, corrected to UUIDv4. Reader rejects invalid plan/inventory/project shape even with recalculated checksum. Native Main download routed to safe temporary directory; bytes reader verifies original metadata/conflict/addition plan. Delayed renderer hash test rejects newer project changes; reset clears download button. FullNode428; scoped review no Critical/Important issue. Packaged validation, refreshed ZIP and UI rollback remain pending.
