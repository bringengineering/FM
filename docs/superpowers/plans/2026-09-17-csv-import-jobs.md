# CSV import jobs and local rollback

Continue the approved original CRM-04 ImportJob/rollback/source-preservation scope. Durable records on this PC are an implementation step; company Drive source storage and shared audit acceptance remain required. No operational deployment.

1. RED tests for immutable account-scoped job manifests and exact original bytes, checksums, collision/tamper/bounds/account separation and rollback protection.
2. Implement csv-job-ledger.js: source CSV plus normalized immutable job in generated .pending-UUID staging folder, atomically published as UUID after both files complete below userData/account; fixed filenames only; 8MiB source/16MiB manifest/100jobs/64MiB account cap. Serialize operations, no silent pruning. Validate all reads and hashes. Reject linked root/account/job folders. Interrupted stages remain visible/count against quota without blocking complete jobs. Receipt files max2MiB; replacement also enforces account byte cap. History IPC sends summary source fields/counts only. Source never overwritten or deleted on rollback. Receipts are explicitly renderer-reported local actions, never cloud proof.
3. Main service reruns existing guarded CSV preview using owned ordinary bytes and compares reviewed payload; local skip IDs must be a unique subset of proposed additions. Create durable normalized drafts manifest before browser mirror changes. Fresh approval/session guard before and after IO; originating project rechecked immediately before browser insertion/deletion; no new-account disclosure.
4. Main/preload create/list/get/receipt IPC; native history shows sources and allows explicit reloading only missing local drafts with fresh shared/project validation. No auto write to CRM.
5. Pure rollback inspector compares every imported local draft against stored hash, blocks missing/edited/shared rows and remaining revisit dependencies. Native rollback checks unchanged local snapshot and form dirtiness before removal; records a renderer acknowledgement afterward. Receipt failure is surfaced separately; all source/manifest data stays recoverable.
6. Node, actual Main/preload and native application/relaunch/history/recovery/rollback tests; scoped reviewer; source package and Windows review build. Preserve 61 gates without asserting broad operational acceptance.
