# Latest local restore rollback UI
Requirement: execute pure rollback helper via CRM review. Authorized scope local project drafts only; no shared database delete or Drive operation. Retain manifest download after rollback.
Design: paginated review displays imported addition IDs/titles only and explains local draft effect. Guard all unsent forms, hidden project form caches, baseline drafts, project saves, uploads and workflow/network pending. Preserve pre-existing dirty state, reject additions already saved. Validate exact complete after hashes before review and again afterward, with epoch/manifest/selected/local snapshot/pending rechecks across hashing. Reset cancels pending review/jobs and disables button. Pending hook participates in CRM logout guard. Apply by removing exact imported additions and restoring before snapshots; select manifest prior project, retain original dirty flags.
Tests first native missingbutton fail; cancel retains additions; execute removes only imported draft, retains Main original/no shared addition, manifest downloadable; unsent form blocks; saved/modified addition blocks; stale/delayed/reset guard. FullNode/native, review, commit/build. Packaged/delivery pending. General uploaded manifest replay/shared restoration not covered by latest-session action.

Checkpoint: absent-button native test failed before implementation. Native cancel/execute restores prior local selection and preserves pre-existing dirty state and Main shared original; latest manifest remains downloadable. Unsent form, saved addition and newer project during delayed hash are rejected. Session reset disables controls/clears pending jobs. FullNode428; independent review no Critical/Important finding. Rollback paging is implemented;101-item rollback paging and delayed post-confirm/reset still require further scoped checks. Packaged/delivery pending.

Packaged checkpoint:0a8c465 x64,80 source files byte-identical ASAR. Added separate native/packaged rollback-edge test:101 rows50/50/1/previous/cancel retain102 projects, new input during post-confirm digest refuses removal, reset during confirmation preserves actual Main original. Independent scoped test review no Critical/Important issue. Full packaged restore/observation/follow-up/baseline/context/disabled checks pass. Refresh archives with runtime evidence. Company shared restoration/general manifest replay remain pending.
