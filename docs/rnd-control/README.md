# BRING R&D 통합 관리 — CRM 통합 개발본

## 구현 위치

기존 BRING CRM src/index.html·app.js에 R&D 통합 관리 메뉴와 전용 화면을 연결했다. 신규 기능은 desktop-crm/src/rnd-control 폴더에 있다. preload.js는 R&D 읽기·저장 IPC만 노출하며, 인증 토큰은 main.js와 repository.js 안에서 처리한다.

R&D 원장은 별도 rndControl/projects·rndControl/visits이다. 기존 CRM 원장을 변경하지 않는다. 프로젝트별 CRM 고정 자료는 허용된 읽기 전용 메타데이터와 선택 원장 ID만 R&D 경로에 보관한다. 승인 사용자는 기존 crmCompany/access와 별도 rndAccess의 활성·이메일·역할 일치를 확인한다. Firebase Functions를 추가하거나 배포 금지 정책을 변경하지 않았다.

## 현재 기능

7개 영역·23개 산출물, 목표·담당·기한·기준·결과·한계·버전 근거, TRL 개발 계획·판정 초안·수정 이력, 운영 기준선 입력과 시간·비용 검증, 프로젝트 생성·전환·공유 목록 조회, 개별/전체 프로젝트 파일 보관·복원.

읽기·저장은 기존 CRM 세션을 사용한다. 원격 저장은 변경 경로를 묶은 원자적 PATCH와 서버의 revision 증가 조건으로 충돌을 차단한다. 서버에서 버전 충돌로 거부한 경우 최신 revision을 다시 읽어 충돌을 안내하고 덮어쓰기 재시도를 하지 않는다. 공유 연결 실패 시 성공 표시를 하지 않는다. 서버 Rules는 익명·미승인·이메일 불일치·viewer 쓰기·수정자 위조·오래된 revision·삭제를 거부한다. Rules는 개발 파일에만 반영했고 운영 배포는 하지 않았다.

실험계획 작성·지정 검토자 승인·계획 동결·실험 시작/종료·고정 데이터 버전·근거 검토·승인 근거에 연결된 관리자 사업 결정을 추가했다. 자기검토 여부·사유와 서버 세션의 수정자를 기록한다. 일반 공유 저장은 연구 기록 변경을 거부하며 Firebase의 빈 배열 생략과 숫자 키 배열 표현을 동일하게 비교한다. 직접 DB 요청도 지정 검토자 승인·동결 계획 변경 금지·승인 근거 수정 금지·결정/감사이력 삭제 금지 규칙으로 검사한다. 이 규칙은 Emulator 시험을 통과했으며 운영에는 아직 배포하지 않았다.

## 실행·시험

desktop-crm에서 npm ci, node node_modules/electron/install.js, npm start. 운영 로그인 실행 전 시험 설정과 변경 범위를 확인한다.

자동 시험: desktop-crm에서 npm test — 351개 PASS.
앱 시험: desktop-crm에서 node test-rnd-electron.mjs — CRM 첫 실행 안내 닫기, 메뉴·모듈 초기화·시험 저장·신뢰한 수정자·목록·메뉴 전환 보존, 전용 IPC의 실험 승인·동결·실행·종료·근거 승인·사업 결정, 앱 내부 프로젝트 입력창·프로젝트 전환 시 미공유 상태와 연구 화면 분리 PASS. 실제 원격 저장을 시험한 결과가 아니다.
서버 시험: 저장소 루트에서 firebase emulators:exec --only database --project demo-bring-rnd --config rnd-emulator.json "node desktop-crm/test-rnd-rules.mjs" — 허용·거부 요청 시험 PASS. permission_denied 경고는 기대한 거부 시험 로그다.

## 남은 작업

Drive 회사계정 인증·폴더 소유/쓰기 확인·프로젝트/자료/버전 경로·해시·동일 버전 재시도 서비스를 추가했다. company-site/public/rnd-drive-auth와 firebase-public/rnd-drive-auth는 새 인증 페이지이며 아직 회사 호스팅에 배포하지 않았다. 회사 Drive 연결 버튼을 누르면 이 공식 호스팅 페이지를 통해 사용자가 인증한다. 토큰은 메인 프로세스 메모리에만 보관하며 CRM 인증 계정에 묶고 50분 뒤 재연결을 요구한다.

남은 작업: 새 인증 페이지와 R&D 서버 Rules의 회사 환경 반영, 실제 Drive 인증·보관권한·원본 업로드 및 두 승인 계정 실공유 시험, 동시 업로드 시 파일/폴더 중복 처리 강화, 데이터셋 manifest·자료 관계·복구·61개 검수의 미완료 항목, 회사 배포 소스와 병합·릴리스 검수. 원본 파일 업로드 후 연구 DB 등록은 별도 단계이며 실패 시 원본 파일의 ID로 재연결한다.

현재 TRL·산출물 승인·이력은 연구 초안이며 공식 외부 평가나 승인 인증 기록을 의미하지 않는다. 실제 운영 준비 완료가 아니다. 기존 Drive FIELD 코드에서 회사 OAuth·루트 폴더 설정이 존재함을 확인했으며 이를 근거로 재사용 구조를 개발한다. 개인 토큰이나 운영 계정을 임의 생성하지 않는다.

버전·업데이트 채널·회사 설치 앱은 변경하지 않았다. 배포·운영 Rules 반영 전에 변경 패키지와 미완료 항목을 검토한다. 롤백은 신규 메뉴·IPC·모듈·rndControl 규칙 변경을 되돌리는 방식이며 기존 CRM 원장 마이그레이션은 없다.

## 원래 기획서 검수 대조

acceptance-audit.md와 acceptance-status.json에 61개 항목을 유지한다. 부분 구현 또는 좁은 단위 시험만으로 전체 항목에 PASS를 부여하지 않는다. 운영 출시는 이 검수와 실제 두 계정 Drive 시험을 별도로 통과해야 한다.

## 가설 관리 추가

명시적으로 가설 체계 버전과 지정 검토자를 입력하면 기획서 H0~H7을 NOT_TESTED 템플릿으로 만든다. 가설 ID는 프로젝트·체계 버전·코드를 포함한다. 실험은 선택한 체계에 연결되며, 다른 체계의 동일 코드 근거로 판정하지 않는다. 지정 검토자/관리자만 해당 가설의 승인 근거를 연결해 판정할 수 있다. 판정은 원래 템플릿을 교체하지 않고 별도 불변 기록으로 쌓으며 근거 버전·이유·검토자·시각을 보존한다. 실험 종료나 사업 결정은 가설의 자동 확정을 일으키지 않는다.

## 승인 계획 변경 버전

승인·실행·종료한 계획에서 변경 초안을 만들면 새 실험 ID와 계획 버전이 생성된다. parentExperimentId/rootExperimentId/parentPlanVersion으로 원계획을 연결하고 changeReason/appliesTo를 필수 기록한다. 가설 코드·체계는 유지하며 기준·기간·대상·측정 방식은 새 초안에서 변경한다. 이전 계획의 승인·동결본·실행 상태·데이터·근거는 교체하지 않는다. 새 버전은 승인이나 실행 시각을 물려받지 않으며 지정 검토자의 재승인 후 실행한다. 이 흐름과 원계획 변경 거부는 모델·CRM 앱·Emulator에서 검증했다. 실제 운영 데이터셋 manifest 동결과 바이너리 보관 검증은 아직 남아 있다.

## 데이터셋 동결 개발

산출물 보관 파일의 프로젝트·자료·versionId·providerFileId·SHA256 연결을 보존한다. 동결 시 Main은 회사 Drive 파일 소유권·공용 root 경로·appProperties 버전과 실제 바이트 해시/크기를 다시 확인한다. 최대 20MiB 바이트 스트림에 제한을 적용한다. 모델은 대상·포함/제외 기준·수집 기간·독립 대상 ID·행수·파일 버전을 필수 검증하고 동결 계획 해시/버전 및 manifestHash를 생성한다. 파일 수·행수·독립 대상 수는 구별한다. 동결 데이터셋을 선택해 종료하면 근거 source에 해당 snapshot ID와 manifestHash가 유지된다. 서버 Rules는 동결 레코드 변경/삭제와 잘못된 계획 참조를 거부한다.

351개 단위 시험·Emulator의 동결/종료/근거 연계 및 변경/삭제 거부·CRM의 모의 업로드 문맥 시험이 통과했다. 실제 Drive 회사 계정의 바이트 시험은 미실행이다. Rules는 공급자 바이트를 읽거나 해시를 계산할 수 없으며 이 검증은 Main에서 수행한다. 기존 링크 기록은 미동결 표시하고 과거 버전 시험을 유지했다. 대외용 자료 확정 시 동결 근거 강제·가변 링크 제외, 중앙 ArtifactVersion 원장과 재검증 상태·자료 복구는 아직 남아 있다.

## Markdown 기록 내보내기

CRM의 내부 기록/검토용 선별 Markdown ZIP 버튼은 공유 저장된 프로젝트 ID와 revision만 Main에 전달한다. Main은 CRM 세션으로 다시 공유 원장을 읽고 파일 선택 후 revision/세션을 재확인해 ZIP을 보관한다. 기기 미공유 초안은 먼저 저장해야 한다. ZIP은 신규 별도 파일이며 기존 Vault 문서를 변경하지 않는다. 프로젝트·가설·실험·데이터셋·근거·결정 노트의 고유 ID·원장 revision·상대 링크와 manifest의 각 파일 SHA256/크기를 포함한다. 자유 입력은 코드 블록으로 표시하고 위험/인증값 포함 URL 및 알려지지 않은 속성은 내보내지 않는다.

내부 기록은 미승인·미동결 자료를 상태와 함께 보존한다. 검토용은 미승인·공개 범위 미확인·동결 manifest 불일치 근거를 제외하며, 포함할 보관본은 Drive에서 재확인한다. 관리자가 공개 범위를 승인한 유효한 동결 근거만 검토용에 포함하며, 승인 취소나 내용 변경 시 제외한다. 원본 바이너리를 포함하지 않으며 전체 파일 백업이 아니다. 실제 공유 원장의 실Drive 검토용 출력은 아직 검증하지 않았다. 기존 기능 포함 351개 테스트, native 출력 IPC/선택 파일 보관, 생성 ZIP의 CRC·한글·manifest 해시·상대 링크를 확인했다.

## 관리자 검토용 자료 포함 승인

전용 공개 범위 form은 관리자만 allow/revoke를 확정할 수 있다. allow에는 근거 및 연결 실험·데이터셋 전체의 공개 권한/민감 정보 검수 확인과 이유가 필요하다. 해당 근거가 승인되어 있고 동결 manifest와 계획 버전이 유효해야 한다. bundleHash는 근거·실험·데이터셋의 전체 기록에 묶인다. 포함 승인·취소는 sourceRevision/sequence/승인자/시각/이유를 가진 별도 불변 기록으로 쌓이고, 취소는 원래 승인 ID를 참조한다. 서버는 관리자 여부·원장 revision·버전 참조와 이력 수정/삭제를 검사한다.

검토용 export는 최신 승인이 allow이고 현재 bundleHash가 일치하는 근거만 포함한다. 연결 자료는 같은 승인 범위에 포함되며 프로젝트 제목·TRL 판정·사업 결정은 별도 공개 범위 승인 없이 검토용에 내보내지 않는다. Drive 원본 확인은 출력 직전에 다시 수행한다. 공개 포함 승인은 Drive ACL 변경·외부 전송이 아니며 이미 내보낸 파일을 회수하지 않는다. 실제 회사 Drive 공개 범위 검수·출력은 아직 미실행이고 가정/문헌/실측 분류와 자료 복구는 계속 구현해야 한다. 351개 테스트 및 Emulator의 저장 원장 재조회/해시 일치/allow·revoke 선별, native 미동결 근거 거부를 확인했다.

## 근거 출처 유형과 개선율 계산

기존 기록은 미분류로 유지한다. 실험 근거에서 실측·문헌·시뮬레이션·가정을 명시하고 출처/측정/계산 범위를 기록한다. 실측 유형은 유효한 동결 데이터셋과 BRING 직접 측정 확인이 필요하다. 실험과 독립적인 문헌/시뮬레이션/가정 참고 근거는 원출처 URL·버전·한계·검토자를 기록한다. 참고 자료의 독립 실측 표본 수는 0이며 실측으로 자동 변환하지 않는다. 유형·설명·직접 측정 확인은 원장에 불변 기록하며 Markdown에서 표시한다. 서버의 문헌 근거 검토/유형 변경 거부 시험이 통과했다.

시간·원가 개선율 초안 계산은 미측정, 비유한 수치, 0 기준값, 계산 범위 초과를 사유와 함께 —로 표시한다. 실제 0 결과는 유효한 측정값이며 10→0은 100% 감소, 10→12는 -20% 감소로 표시한다. 계산 결과를 metricResults 원장에 기준·결과 데이터셋 ID/해시, 단위, 원본 위치, 계산 버전과 함께 보존한다. 실측 개선 주장은 해당 실험의 유효한 수치 결과 연결이 필요하다. 참고 자료는 현재 내부 기록에 포함되며 검토용의 참고 자료 스냅샷 공개 범위 승인 확장은 남아 있다. 기존 기능 포함 351개 테스트 및 native 계산 화면 검증을 확인했다.

## 수치 결과 원장 추가

기준·결과 동결 데이터셋을 선택하고 수치·단위·원본 위치·측정 방법·한계를 기록한다. Main이 개선율을 계산하고 서버 Rules도 원본 해시와 계산식을 검사한다. 수치 결과는 수정/삭제 대신 별도 기록으로 보존하며 근거에 연결한 ID 목록의 불변 복사본을 대조한다. 공개 포함 승인 해시에 수치 결과와 기준 자료도 묶고, 검토용 출력 전에 기준·결과 원본 모두 Drive에서 재확인한다. Markdown에는 수치 노트와 양쪽 자료 링크를 포함한다.

현재 수치는 사람이 원본에서 전사하는 방식이며 CSV 자동 추출이나 실제 측정 정확도를 검증한 것은 아니다. 실제 회사 Drive/두 계정 시험, 원래 61개 검수는 계속 필요하다. 기존 CRM 기능 포함 351개 자동 시험과 Emulator의 수치 원장 저장·해시/계산식 오류 거부를 확인했다.

수치 연결 목록은 최대 100개이며 서버가 기존 항목의 삭제·교체·추가를 거부한다. 두 수치 결과를 연결한 근거에서 한 항목만 삭제하거나 새 항목을 추가하는 직접 DB 요청 거부를 Emulator에서 확인했다. 삭제 시 하위 validate가 생략되는 Firebase 동작을 고려해 상위 쓰기 조건에서도 기존 연결 보존을 검사한다.

## 첫 프로젝트 시작

최초 R&D 화면은 프로젝트를 자동 생성하지 않는다. 새 프로젝트 입력을 취소하면 원장·세션 프로젝트가 남지 않는다. 이름을 입력해 생성하거나 공유 목록을 조회해 선택한 뒤 산출물·TRL·연구 화면을 사용한다. CRM 앱에서 빈 상태→취소→생성→23개 산출물→공유 저장과 기존 연구 흐름을 확인했다. 실제 빈 운영 workspace 시험은 아직 미실행이다.

## Windows 패키징 검증

`npx electron-builder --win --dir --x64 --publish never`로 기존 1.8.0 설정의 Windows x64 실행 파일 패키징을 완료했다. `BRING_RND_PACKAGED_EXE`에 `dist/win-unpacked/BRING CRM.exe` 절대 경로를 설정해 `node test-rnd-electron.mjs`를 실행했고 같은 CRM 업무 시험을 통과했다. 이 시험은 로컬 메모리 원장과 모의 업로드를 사용하므로 운영 DB/Drive 증거가 아니다.

별도 `BRING_RND_Windows_검토빌드_20260917.zip`에 실행 파일 전체와 파일별 SHA256/크기 manifest, 빌드/실행 시험 로그를 보관했다. 운영 릴리스·자동 업데이트 게시·설치 실행은 하지 않았다. 기본 appId/버전은 기존 CRM과 같으므로 회사 설치본을 대체할 최종 릴리스는 남은 검수 후 별도로 만든다.

## 저장 응답과 초안 보존

프로젝트 저장은 요청 시점의 프로젝트 복사본을 사용한다. 응답 대기 중 프로젝트를 전환해도 현재 선택을 바꾸지 않는다. 요청 이후 추가 수정이 있으면 응답으로 덮어쓰거나 미공유 표시를 지우지 않는다. 프로젝트별 중복 저장 요청을 차단한다. 최신 공유본 비교/재적용 화면은 계속 필요하다. 개발 소스와 재패키징된 Windows 실행 파일에서 지연 응답·전환·추가 수정 시험을 통과했다.

## 공유본 비교와 선택 재적용

최신 공유본과 제목·산출물/TRL의 목표·담당·기한·기준·결과·검토자 차이를 표시한다. 기본 선택은 없으며 이유를 입력해 선택 수정만 최신 revision의 초안에 적용한다. 상태·근거·판정·연구·승인 이력은 최신 공유본을 유지한다. 기존 공유 감사 이력 뒤에 비교 revision/사유/로그인 사용자와 재적용 항목을 추가한다. 재적용 전 초안은 현재 세션에서 파일로 보관할 수 있다. 반영은 공유 저장이 아니며 저장 시 기존 서버 충돌 검사도 유지된다. 351개 자동 시험 및 CRM 비교 화면 시험을 확인했다. 실제 두 계정 동시 편집 검수는 남아 있다.

## 선택 기록 직접 조회

전용 rndGet IPC는 projects/visits와 유효 ID만 받는다. Main이 로그인·역할·비밀번호 변경 상태를 검사하고 선택 기록 경로만 읽는다. 저장소는 토큰 준비 후와 응답 수신 후 세션을 다시 확인하며 반환 ID도 대조한다. 연구 조회·공유본 비교와 기존 Main의 연구 작업/출력은 단건 조회를 사용한다. 전체 목록은 목록 조회에만 사용한다. 이 변경은 전체 검색·10,000건 페이지 처리 검수 완료를 의미하지 않는다.

## 복원 사전 검증과 충돌 유지

개별/묶음 JSON 복원은 파일 구조·프로젝트 ID/revision·9개 고유 TRL 단계·중복 프로젝트를 검증한다. 신규/동일 ID 수를 먼저 표시하며 확인 후 신규 프로젝트만 세션 초안에 추가한다. 기존 동일 ID 프로젝트를 덮어쓰거나 공유 DB에 바로 기록하지 않는다. 기존 수정은 유지하며 새 초안은 미공유 표시한다. 모델과 CRM 앱에서 동일 ID 충돌 유지 시험을 확인했다. 전체 연구 관계 검증·이력 병합·바이너리 복구와 영속 롤백 manifest는 아직 별도 개발이 필요하다.

## 복원 연구 관계 검사

JSON 복원 사전 검증은 연구 레코드 고유 ID·프로젝트 ID, 변경 실험의 부모/계획 버전, 데이터셋 실험/계획 참조, 수치 결과의 기준·결과 자료 해시, 근거의 실험/자료/수치 참조, 판정·결정의 근거 참조, 공개 승인의 자료/선행 승인 참조를 검사한다. 파일 안에서 연결이 끊어진 경우 적용하지 않는다. 이는 Drive 실제 바이트 검증이나 원래 승인자 신원 재인증을 대신하지 않으며, 운영 원장 복구 승인과 바이너리 복원 시험은 남아 있다.

## 필수 산출물 준비율

산출물 편집에서 필수/선택을 명시하고 변경 이유와 함께 기록한다. 종합 현황의 준비율은 필수 산출물 승인/필수 수로 계산하며 선택은 분자·분모에서 제외한다. 필수가 0개면 —이다. 기존 구분 없는 산출물과 신규 23개 기본 템플릿은 필수로 취급하며 사용자가 기준에 따라 선택으로 변경한다. 이 준비율은 기술 성숙도나 외부 평가 점수가 아니다. 필수 4개 중 1개 승인 및 선택만 승인한 경우의 모델 시험, CRM 구분 저장·22개 분모 표시를 확인했다.

## 미완성 실험 초안 모델/서버

전용 createDraft 작업은 실험 ID·가설 코드와 체계 관계를 검사하고 미완성 계획을 draft로 보관한다. editDraft는 작성자/관리자의 명시적 이유와 허용 필드만 받아 보완하며 계획 버전은 유지한다. 이전·이후 계획과 이유를 연구 감사 이력에 추가한다. 승인에는 전체 필수 계획 항목이 필요하며 승인된 계획의 변경은 기존 변경 버전 흐름을 사용한다. 서버는 draft 유지 변경을 작성자/관리자에게만 허용하고 승인 시 필수 항목·지정 검토자·동결본 일치를 검사한다. 모델/Emulator 시험을 통과했다. 미완성 초안 저장·보완 전용 화면을 연결했고 기존 전체 계획 등록도 유지한다.

CRM의 미완성 실험계획 저장·보완 화면에서 ID·가설만으로 초안을 저장하고 기록의 초안 보완 버튼으로 입력을 불러온다. 기존 초안의 ID·가설은 편집 중 잠근다. 실제 CRM 시험에서 질문 보완 저장과 필수 항목 누락 승인 거부를 확인했다. 이 앱 시험은 로컬 시험 원장이며 회사 공유 DB 시험을 대신하지 않는다.

## 릴리스 상태 확인

`npm run rnd:release:check`는 원래 61개 ID를 고정 대조하고 PASS/근거 목록을 검사해 release-readiness.json을 생성한다. 미완료·근거 누락 시 종료 코드 1이다. 현재 4/61 근거 포함 PASS이며 운영 릴리스 준비가 아니다. 기록된 근거의 존재와 실제 내용·실계정·배포 검수는 사람이 별도로 대조해야 한다. `npm run build:rnd:review`는 게시 없이 Windows 검토용 실행 파일만 만든다. 기존 회사 릴리스 명령/업데이트 채널은 변경하지 않았다.

다음 우선순위는 별도 rndAccess 승인, 로그인/로그아웃 시 초안 정리, 동시 업로드 복구, 자료 원본을 포함하는 복구 시험이다. 실제 회사 계정 시험과 61개 종합 검수가 완료되기 전 운영 준비 완료로 표시하지 않는다.

## 별도 R&D 서버 접근 승인

R&D DB 읽기·쓰기는 CRM 승인과 rndAccess 승인을 모두 요구한다. 활성 상태·로그인 이메일·CRM 역할이 일치해야 한다. 기존 CRM 관리자가 별도 승인을 부여/취소하며 approvedBy/approvedAt/reason을 기록한다. 실무자의 자기승인과 역할 위조·승인 삭제는 거부한다. Emulator에서 CRM만 승인된 계정, 별도 승인 비활성·역할 불일치 거부와 관리자 승인/취소를 확인했다. Rules는 아직 회사 서버에 배포하지 않았다. Main/Drive 사전 승인 확인과 관리자 편집 화면·운영 승인자 초기 설정은 계속 필요하다. 기존 사용자에게 R&D 권한을 자동 부여하지 않는다.

## CRM/Drive 작업 전 승인 확인

Main의 목록·단건 조회·저장·연구 작업·내보내기와 Drive 인증/업로드는 별도 R&D 승인을 조회한다. 조회 전후 세션 UID/이메일/역할을 확인하며 활성·CRM 역할 일치를 요구한다. 인증 대기와 파일 선택 이후 다시 승인/계정을 확인한다. 확인 실패 시 Drive 메모리 연결을 지운다. 로컬 앱 시험은 외부 승인 DB를 호출하지 않는 명시적 시험 모드이므로 승인 서비스 단위/서버 Emulator 시험과 구분한다. 실제 회사 승인 DB/Drive 시험과 관리자 UI·로그아웃 정리는 아직 필요하다.

## 관리자 승인 화면

CRM 관리자에게 사용자 UID 기반 R&D 승인 조회·활성/취소·사유 입력 화면을 표시한다. 전용 Main IPC는 관리자와 대상 ID를 검사하고 서비스가 공유 CRM 이메일/역할을 다시 조회한다. 입력한 역할값을 승인 역할로 사용하지 않는다. 승인 전후 세션을 검사하며 서버 Rules도 CRM 역할 일치를 검사한다. 로컬 앱 시험은 현재 시험 관리자만 대상으로 허용하고 실제 회사 승인을 변경하지 않는다. 서비스의 역할 위조 무시/비관리자 거부와 CRM 승인 저장 화면 시험을 확인했다. 운영 승인 이력 별도 원장·동시 관리자 변경 대조·실회사 계정 시험은 남아 있다.

## 관리자 승인 충돌 처리

승인 조회는 Firebase ETag를 받으며 저장은 조회한 ETag의 If-Match 조건으로만 요청한다. 대상 UID를 변경하거나 저장 후에는 다시 조회해야 한다. 412 충돌은 재조회 안내로 처리하고 덮어쓰기 재시도를 하지 않는다. 기존 관리 화면은 현재 승인 조회 후 저장하는 순서다. 조건부 쓰기 헤더·충돌 거부/단일 요청 서비스 시험과 CRM 조회→저장 화면 시험을 확인했다. 두 실관리자 동시 요청의 회사 서버 시험과 승인 불변 감사 원장은 남아 있다.

## 좁은 화면과 긴 식별자

390×844 및 1440×1000 화면에서 R&D 콘텐츠를 확인했다. 긴 해시/ID는 줄바꿈하며 입력·버튼·그리드의 최소 너비가 콘텐츠를 밀어내지 않도록 한다. 비교 대화상자는 화면 높이 안에서 스크롤하고 체크박스를 별도 정렬한다. CRM 앱/Windows 실행 파일에서 300자 식별자와 비교 제목을 넣어 콘텐츠/대화상자 가로 넘침과 높이를 검사했다. 좁은 콘텐츠·비교 대화상자 이미지를 패키지에 보관한다. 전체 모바일 업무 완료/실사용자 시간 검수는 이 검사와 별도로 남아 있다.

## 승인 감사 기록 모델

access-audit.js는 승인 변경 요청의 이전/요청 상태·대상 UID·실제 관리자·사유·ETag·시각·eventHash를 생성한다. 기록은 approval-attempt이며 committed=false로 시작한다. 비관리자·수정자 불일치·대상/원본 버전 누락을 거부한다. 이 모델은 아직 원격 저장 서비스에 연결하지 않았다. 요청/성공 기록 분리·불변 서버 원장·현재 승인 auditId 연결·실패 복구/조회 화면을 다음에 구현해야 하며 모델 시험만으로 승인 감사 원장 완료를 의미하지 않는다.

## 승인 요청 감사 서버 원장

신규 rndAccessAudit/대상UID/요청ID 원장은 CRM 관리자만 생성·조회한다. 서버는 관리자 UID·대상·요청 유형·committed=false·현재 승인과 동일한 이전 상태·CRM 이메일/역할과 일치하는 요청 상태를 검사한다. 생성 후 수정·삭제는 관리자에게도 거부한다. Emulator에서 요청 생성·관리자 수정/삭제·실무자 위조 생성 거부를 확인했다. 서비스의 실제 승인 저장/성공 확인·복구 UI 연결은 아직 남아 있다. eventHash 형식은 Rules가 검사하지만 해시 계산은 Node 모델에서 수행한다.

strengthen-rnd-rules.py의 접근 조건을 기존 생성 결과에서 추출하지 않고 원래 CRM 승인 조건으로 생성하도록 수정했다. 반복 실행 결과의 SHA256 일치를 확인했다. 이 수정으로 nullable 프로젝트 필드 저장 거부 회귀를 해결했고 전체 서버 시나리오를 다시 통과했다. 운영 배포는 하지 않았다.

## 승인 저장과 감사 요청 연결

관리자 서비스는 저장 직전 현재 승인 ETag를 재조회·비교하고 관리자 세션에서 요청 ID를 생성한다. 변경 전/요청 상태를 불변 감사 원장에 먼저 저장하며 실패하면 승인 PUT을 하지 않는다. 성공한 감사 요청 ID를 승인 auditId로 연결하고 기존 If-Match 조건부 PUT을 수행한다. 승인 충돌 시 감사 요청은 남지만 committed=false 요청 기록이며 승인 성공 증거가 아니다. 서버는 auditId가 있을 때 관리자·요청 상태 연결을 확인한다. 모델 서비스의 감사 실패/변경 중단과 충돌 단일 요청 시험 및 전체 서버 시험을 확인했다.

이후 서버 감사 강제 검증을 추가했다. 별도 성공 확인 기록·불확실한 저장 복구·감사 조회 UI·실회사 요청 시험은 계속 구현해야 한다. 로컬 관리자 화면 시험은 별도 메모리 원장이고 원격 감사 저장 증거가 아니다.

## 승인 감사 연결 강제

서버는 새 auditId와 현재 승인 이전 값·요청 이후 값 전체가 감사 레코드와 일치하는 승인 저장만 허용한다. 현재 감사 ID 재사용, 감사 ID 누락, 이전 상태 위조 요청을 거부한다. 과거 초기 레코드는 읽을 수 있지만 이후 변경에는 감사 요청을 생성해야 한다. Emulator에서 감사 요청→승인·취소, 감사 없는 직접 쓰기 거부, 감사 ID 재사용 거부와 이전 값 불일치 거부 및 전체 연구 시나리오를 확인했다. 반복 규칙 생성의 결과 SHA256 일치도 재확인했다. 운영 Rules는 아직 변경하지 않았다.

## 현재 승인 감사 연결 조회

관리자 승인 조회는 현재 승인 auditId의 불변 요청 기록을 추가로 읽고 대상 UID·ID·승인 이후 값 전체를 대조한다. 일치하면 조회한 승인과 연결 확인, 불일치면 확인 필요, 감사 ID가 없으면 과거 기록으로 표시한다. 이는 조회 시점의 현재 승인 포인터에 대한 확인이며 변경 요청만으로 성공을 판정하지 않는다. 과거 성공 상태 별도 확정 원장·응답 유실 복구·미연결 요청 목록·실회사 두 관리자 시험은 남아 있다. 서비스 단위 시험과 기존 관리자 화면/Windows 앱 흐름을 확인했다. 로컬 화면 시험은 실제 감사 DB를 조회한 증거가 아니다.

## 승인 응답 유실 재확인

승인 조건부 PUT의 연결/응답 해석 오류가 발생하면 쓰기를 재시도하지 않고 현재 승인만 다시 읽는다. 같은 auditId와 승인 값 전체가 일치하면 재조회로 확인했다고 반환·표시한다. 값 불일치나 조회 실패면 미확정이며 감사 요청 ID와 재조회 안내를 남긴다. 서비스 시험에서 실제 저장 후 응답 유실의 일치 확인과 미저장 응답 유실의 미확정 거부, 각각 승인 PUT 1회를 확인했다. 이것은 프로세스 종료 후 영속 복구·과거 성공 원장·실회사 네트워크 장애 시험 완료를 뜻하지 않는다.

## 감사 요청 ID 결과 조회

관리자 서비스의 inspect와 전용 IPC는 대상 UID/감사 요청 ID를 받아 불변 요청과 현재 승인만 조회한다. 대상/ID 불일치를 거부하며 현재 승인 auditId와 요청 이후 값 전체가 같을 때 연결 확인으로 표시한다. 그 외는 미확정 요청이며 과거 적용 여부를 단정하지 않는다. 관리자 화면에서 감사 요청 ID를 입력해 조회할 수 있고 정상 원격 저장 후 생성된 ID를 입력칸에 보관한다. 로컬 모드는 실제 감사 조회를 거부하며 운영 감사 DB를 모의 성공으로 표시하지 않는다. 프로세스 종료 후에도 서버 요청 ID 조회는 가능하지만 자동 요청 복구·과거 성공 확정 원장과 실회사 장애 시험은 남아 있다.

## 세션 변경 시 R&D 메모리 정리

CRM 인증 상태의 UID·이메일·역할·비밀번호 변경 상태가 바뀌면 R&D 세션 초기화를 발행한다. 프로젝트 목록/초안·재적용 백업·자료 선택·산출물/TRL/연구 입력·기준선 기록·연구 카드와 관리자 표시를 정리한다. 캐시된 R&D 호스트를 명시적으로 참조해 다른 CRM 메뉴에 있을 때도 정리한다. 세션 이후 이전 업로드 응답은 UI 자료 반영을 중단하며 CRM 로그아웃 완료 과정에서 Drive 연결을 지운다. 앱 시험에서 다른 메뉴로 이동→초기화→R&D 재진입 시 프로젝트/연구 기록 0개를 확인했다. 실제 로그아웃 미공유 확인·승인 취소 즉시 UI 정리·진행 중 전체 작업 취소·두 UID 실계정 시험은 아직 남아 있다.

## 로그아웃 전 R&D 입력 확인

CRM 로그아웃 버튼은 미공유 프로젝트·입력 중인 폼·업로드 대기·기준선 입력을 먼저 확인한다. 기존 CRM 확인창에서 돌아가서 보관을 선택하면 로그아웃 API를 호출하지 않고 초안을 유지한다. 적용/저장에 성공한 폼은 입력 대기 표시를 해제한다. 로그아웃 API 성공 뒤 명시적으로 R&D 세션 초기화를 발행하고 페이지를 다시 연다. Main의 기존 FIELD 로그아웃 순서는 유지하며 완료 과정에서 Drive 연결을 지운다. 앱 시험에서 미공유 초안 생성→로그아웃 취소→동일 ID/미공유 유지 확인을 통과했다. 개발자도구의 직접 로그아웃 호출 경고·실계정 성공/취소·진행 중 모든 작업 취소 검수는 남아 있다.

### 저장 대기 중 입력 보호
연구 기록과 접근 승인 폼은 저장 요청 시 입력 세대를 기록하며, 응답 전에 추가 입력한 경우 미공유 표시를 유지합니다. 세션 초기화 이전 요청도 새로운 입력 표시를 지우지 않습니다. 입력 상태 회귀 테스트를 포함한 Node 테스트 352개와 Electron 화면 흐름을 검증했습니다. 실제 두 계정 동시 운영 검증은 별도 진행해야 합니다.

### CRM 계정 전환 보호
실시간 CRM 로그인 알림을 초기 조회 응답보다 우선하며 계정·역할 변경 시 승인 관리 폼을 새로 생성합니다. 이전 세션의 방문 기록 조회·저장·자료 업로드 응답은 새 화면에 반영하지 않습니다. 방문 기록 저장 중 추가 입력의 미저장 표시도 유지합니다. Electron의 Main→preload 로그인 이벤트 경로에서 회원→관리자 전환과 폼 초기화를 검증했습니다. 시험용 이벤트이며 실제 두 회사 계정의 서버 권한 검증을 대신하지 않습니다.

### Drive 업로드 겹침·세션 변경 보호
동일 CRM 프로세스에서 업로드가 진행 중이면 다음 업로드는 명시적으로 거절하고 완료 후 재시도하도록 안내합니다. 폴더 조회·생성, 기존 버전 조회, 업로드·원본 조회마다 요청 시작 시의 Drive 연결을 확인합니다. 연결 해제·계정 변경 후 후속 생성 요청을 중단합니다. 재사용 버전은 해시뿐 아니라 프로젝트·자료 ID·크기·부모 폴더도 비교합니다. 동시 요청과 조회 대기 중 연결 해제 회귀 테스트를 포함해 Node 테스트 353개가 통과했습니다. 여러 PC의 전역 중복 방지·응답 유실 복구와 실제 Drive 통합 시험은 아직 남아 있습니다.

### 연구 메타데이터 검색
프로젝트·산출물·실험·데이터셋·근거·측정 결과·의사결정·가설을 세션 조회본과 초안에서 검색합니다. 이름·담당자·검토자·가설·자료 ID·결과 등을 NFKC 정규화한 복수 검색어 AND 조건으로 조회하고 종류별 필터 및 50개 페이지 이동을 제공합니다. 검색 결과에서 해당 프로젝트를 열고 산출물은 직접 선택합니다. 검색 화면에 조회 범위와 최신 공유본 확인 필요를 표시하며 세션 전환 시 검색어·인덱스를 정리합니다. 10,000개 메타데이터의 검색·페이지 분리·조건·빈 결과 테스트와 Electron 검색 이동을 검증했습니다. 실제 10,000개 원격 자료 성능과 3인 사용성 시험은 아직 남아 있습니다.

### 현재 초안 검색 갱신
검색 인덱스에서 선택 프로젝트의 세션 캐시를 현재 초안으로 대체합니다. 제목 변경 및 초안 반영 때 검색 결과를 갱신하며, 같은 ID를 중복 표시하지 않습니다. Electron에서 미공유 제목 검색과 이전 제목 제외를 확인했습니다.

### 다음 통합 작업 순서
1. OUT-03: 저장소·파일 경로·40자리 commit SHA를 포함하는 코드 근거와 미고정 링크 경고.
2. OPS-06: R&D 메뉴·진입점 비활성화 및 데이터 삭제 없는 복귀 절차.
3. OPS-02/OPS-01: 메타데이터 보관 manifest와 표본 바이너리 해시를 포함한 빈 시험 저장소 복원 검증.
4. 실제 승인 회사 계정 2개와 시험 PC 2대로 권한 취소·Drive 업로드·공유 충돌·원본 복구 검증.
모든 원래 61개 검수 항목은 유지하며, 자동 테스트 통과만으로 수동·실연동 검수 결과를 PASS로 바꾸지 않습니다.

### GitHub 코드 근거 버전 표시
참고 근거에 GitHub HTTPS blob/commit 링크를 등록하면 저장소·파일 경로·40자리 commit SHA 및 고정 여부를 미리 표시합니다. branch·태그·축약 SHA와 저장소 일반 링크는 미고정 경고를 표시합니다. 40자리 링크 SHA와 40자리 출처 버전이 다르면 Main 연구 모델이 저장을 거부합니다. 인증 정보·검색 인자가 있는 GitHub 링크도 거부합니다. Markdown 근거 문서에는 동일한 코드 버전 설명을 포함합니다. 코드가 원격 저장소에 실제 존재하는지·그 내용이 주장을 입증하는지는 별도 검토 대상이며 자동 검증 완료로 표시하지 않습니다. Node 테스트 355개와 Windows 미리보기 경고 시험을 수행했습니다. OUT-03의 실제 저장소 근거 등록 수동 검수는 남아 있습니다.

### 범위 명시 메타데이터 백업
전체 프로젝트 보관은 BRING_RND_METADATA_BACKUP wrapper에 메타데이터 archive와 manifest를 포함합니다. manifest는 생성 시각·프로젝트 개수·revision·엔터티별 개수·동결 데이터셋의 파일 버전 참조 및 archive SHA-256을 기록합니다. binaryFilesIncluded=false와 metadata-only 범위를 고정하고 다운로드 안내도 Drive 원본 미포함을 표시합니다. 가져오기에서 SHA 및 목록·개수를 확인한 뒤 기존 관계 사전 검증을 수행합니다. 과거 version 1 archive는 해시 manifest 없음으로 안내 후 허용합니다. 이는 전자서명·작성자 인증이나 원격 원본의 존재·바이너리 해시 검증을 대신하지 않습니다. 시험은 Node 356개 및 Windows의 WebCrypto 왕복·변조 거절이며, 실제 빈 저장소 원본 복원 검수는 남아 있습니다.

### 자료 조회·백업·복원의 세션 경계
공유 프로젝트 목록 조회, 메타데이터 백업 생성, 복원 파일 읽기·검증 및 새 프로젝트 이름 입력은 시작 시 세션 세대를 캡처합니다. 비동기 대기 중 세션이 초기화되면 이전 결과의 화면·초안 반영 또는 파일 다운로드를 중단합니다. Electron 시험에서 공유 목록 응답을 지연시킨 뒤 세션을 초기화하고 응답을 전달해 이전 프로젝트가 다시 나타나지 않음을 확인했습니다. 실제 두 회사 계정의 원격 권한·자료 격리 시험은 별도 남아 있습니다.

### 복원 연구 관계 검증 보강
복원 사전 검증은 실험 부모·원본 계보와 순환 여부, 근거 수치 ID 목록 중복 및 불변 JSON 연결 원장 일치, 의사결정·가설 검토의 근거 ID 목록과 고정 출처 버전을 확인합니다. 존재하는 검토 source 목록은 개수·고유성·현재 연결 근거 버전이 맞아야 합니다. 과거 source 목록이 없는 기록은 기존 호환 정책을 유지합니다. 실패 시 신규 초안 추가를 수행하지 않습니다. Node 회귀 테스트 포함 357개가 통과했습니다. 실제 원본 바이너리·서버 계정 승인 복원 검수는 별도 남아 있습니다.

### 산출물 이동 중 업로드 결과 보호
자료 업로드 시작 시 프로젝트·산출물 ID를 고정하고 완료 결과를 해당 조합으로 세션에 보관합니다. 이동한 화면의 다른 산출물에는 URL·버전을 쓰지 않습니다. 시작 산출물로 돌아오면 보관된 원본 ID·SHA·크기·버전이 복원되며 초안 반영 시 대기 상태를 해제합니다. 미반영 완료 업로드는 로그아웃 확인의 미완료 작업 수에 포함하고 세션 초기화 시 제거합니다. 지연 응답 뒤 다른 산출물 입력 유지와 원래 산출물 복귀 시험을 수행했습니다. 앱 재시작 후 영구 업로드 복구는 별도 남아 있습니다.

### 전체 R&D 현황
현재 세션의 프로젝트 조회본·초안을 종합하여 필수 승인 개수·준비율·막힘·담당 미지정·진행 실험·근거 검토 대기·미공유 초안을 표시합니다. 준비율은 필수 항목 전체의 승인 수/전체 수로 계산하며 프로젝트별 퍼센트 단순 평균을 사용하지 않습니다. 필수 항목이 없으면 준비율은 미정, 평가하지 않은 TRL은 미평가로 표시합니다. 프로젝트 행 선택으로 상세 화면에 이동하고 앞 50개 표시 후 검색 이용을 안내합니다. 현재 입력폼에 아직 반영하지 않은 필드는 집계에 포함되지 않습니다. 실시간 서버 전체 현황이나 기술성·사업성 자동 판정으로 표시하지 않습니다. 집계 회귀 테스트 포함 Node 358개가 통과했습니다.

### 전체 현황 미공유 표시 갱신
프로젝트 생성·수정·공유 저장의 dirty 상태 변경 직후 전체 R&D 현황을 갱신합니다. 새 프로젝트가 생성되는 순간 미공유 초안 수와 행 표시가 적용됩니다. CRM 화면 시험에서 생성 직후 미공유 1개·미평가 TRL 표시 및 요약 행의 프로젝트 이동을 확인했습니다.

### R&D 기한 경과 집계
전체 현황과 프로젝트 행에 기한이 지난 미승인 산출물 수 및 그중 필수 산출물 수를 표시합니다. 기준일은 해당 PC 현지 날짜로 함께 표시합니다. YYYY-MM-DD 실제 날짜만 비교하고 승인 완료·당일 기한·기한 없음·잘못된 날짜는 경과 수에서 제외합니다. 기한 경과는 상태나 TRL을 자동 변경하지 않습니다. 기준일 고정 회귀 시험을 포함해 Node 359개가 통과했습니다.

### 실제 연구 저장 필드 검색 보강
색인은 실제 저장 필드 datasetSnapshots·hypotheses를 사용합니다. 가설 판정·공개 범위 검토 종류도 포함하고 실험 plan의 담당자·가설 등 허용 텍스트와 근거 주장·한계·가설 문장·데이터 모집단·선정 규칙·버전·검토 UID를 검색합니다. 인증 정보나 전체 중첩 객체를 무차별 색인하지 않습니다. 실제 저장 필드 fixture를 대상으로 데이터셋·가설·실험 계획·판정·공개 검토 누락 회귀 시험을 추가했으며 Node 360개가 통과했습니다. 원격 색인 전체 범위·대용량 검수는 아직 남아 있습니다.

### 실험 필수 계획 텍스트 검색
실험 검색은 모델의 필수 계획 텍스트인 가설·연구질문·비교 대상·모집단·기간·측정지표·분석·성공·중단 기준·검토자 UID를 포함합니다. 별도 token 같은 허용하지 않은 속성은 색인하지 않습니다. 각 실제 필수 텍스트 검색과 비허용 속성 제외 회귀 시험을 포함해 Node 361개가 통과했습니다.

### Drive 원본 검증 연결 고정
원본 해시 검증은 시작 시 Drive 연결 객체를 고정합니다. 동일 UID·동일 token으로 재연결해도 이전 검증의 결과를 사용하지 않습니다. 다운로드 대기 중 같은 계정 재연결 뒤 이전 바이트 응답을 반환하는 회귀 시험에서 세션 변경 거절을 확인했습니다. Node 362개가 통과했습니다. 현재 호출 가능한 Drive 커넥터가 없으며 실제 회사 Drive 통합 검증은 미실행입니다.

### 재시작 후 업로드 복구 원장
Main이 회사 Drive 업로드를 완료하고 계정 승인을 다시 확인한 뒤 현재 UID별 userData/rnd-upload-recovery 원장에 프로젝트·산출물·버전·원본 ID·SHA·크기·업로드 당시 검증 시각을 원자적으로 기록합니다. 토큰·파일명·임의 입력 속성은 보관하지 않습니다. 동일 조합의 다른 원본/해시/크기는 충돌로 거부하고 기록 오류 시 Drive 파일 ID와 오류를 반환합니다. 현재 승인 계정만 IPC로 자기 기록을 조회하며 화면은 과거 검증 기록이라고 표시합니다. 같은 PC 원장 최대 1,000개이며 기록을 임의 삭제하지 않습니다. 재시작 후 읽기·계정 분리·토큰 제외·충돌 거부 회귀 시험을 포함해 Node 363개가 통과했습니다. 현재 원본 재검증 후 산출물에 재적용하는 기능·중간 실패/여러 PC 통합 복구는 아직 남아 있습니다.

### 복구 원본 재검증·산출물 연결
복구 기록의 '원본 재검증 후 초안 연결'은 Main에서 현재 계정의 복구 원장·공유 프로젝트 산출물 소속을 확인하고 Drive 회사 소유권/공용 경로/버전/실제 바이너리 SHA·크기를 재검증합니다. 각 비동기 단계 이후 승인·UID를 재확인합니다. 검증 성공 시 시작 프로젝트·산출물 키로 세션 입력 자료를 보관하며 현재 다른 화면은 바꾸지 않습니다. 초안 반영과 공유 저장을 자동 수행하지 않습니다. 실제 RTDB keyed map 산출물 형식도 확인합니다. Node 회귀 시험 포함 364개가 통과했습니다. 로컬 시험 IPC는 실제 복구를 명시 거절하며 실제 회사 Drive 원본 복구·두 PC 검수는 여전히 미실행입니다.

### 복구 완료 전 공유본 재확인
원본 검증이 완료된 후 공유 프로젝트를 다시 읽고 시작 revision·산출물 소속이 유지되는지 확인합니다. 변경·삭제 시 결과 적용을 거부하고 재조회를 안내합니다. 복구 응답은 원장 프로젝트·산출물·버전·원본 ID·해시·크기·새 검증시각만 허용하며 URL을 Drive 원본 ID로 구성합니다. 검증 서비스 임의 속성이나 인증 정보가 응답에 섞이지 않습니다. 초기 산출물 누락·다운로드 후 revision 변경·반환 메타데이터 제한 회귀 시험을 포함해 Node 366개가 통과했습니다. 실제 원격 회사 Drive 복구 시험은 아직 남아 있습니다.

### 복구 원장 읽기 검증
복구 원장 조회는 2MiB 파일 한도를 확인하고 프로젝트·산출물·버전 조합의 중복을 거부합니다. 잘못된 원장은 읽기 오류로 표시하며 삭제·덮어쓰지 않습니다. 같은 버전 재기록은 기존 원본·해시·크기 일치를 확인해 한 기록을 유지합니다. 중복/과대 원장 거부와 원본 파일 유지 회귀 시험을 포함해 Node 367개가 통과했습니다. 이는 로컬 파일 작성자 인증이나 여러 프로세스 파일 잠금을 대신하지 않습니다.

### R&D 대화창 세션 정리
세션 초기화 시 열린 R&D 전용 입력·비교 dialog를 닫습니다. 새 프로젝트 입력은 취소 결과로 정리되어 이전 이름으로 초안을 재생성하지 않습니다. 최신 공유본 비교는 로그인 조회 전후·공유본 조회 후·선택 재적용 때 시작 세션을 확인하고 달라진 결과를 사용하지 않습니다. Electron 시험에서 열린 이름 입력 중 세션 초기화·dialog 제거·이전 프로젝트 미생성 및 다음 세션 새 입력을 확인했습니다. 실제 두 회사 계정의 전환 검수는 별도 남아 있습니다.

### 100MiB 파일 정책·실행파일 차단
기획 제안에 맞춰 회사 CRM 업로드·Drive 원본 확인·동결 DS 참조·복구 원장 크기 한도를 파일당 100MiB로 정렬했습니다. Main에서 승인 확장자·크기를 확인한 후 읽고 Drive 서비스가 실제 내용의 실행파일 헤더도 확인합니다. HTML·SVG·매크로 문서는 원본 자료로만 보관하며 앱 안에서 실행하지 않습니다. 큰 파일·미지원 형식은 승인 저장소 링크 등록으로 안내합니다. 내보내기 메타데이터 ZIP 한도 20MiB는 별도 유지합니다. 정책 경계·실행파일/위장 원본 회귀 시험을 포함해 Node 368개가 통과했습니다. 실제 100MiB 전송 성능·여러 PC 동시 업로드 시험은 미완료입니다.

### 큰 파일 헤더 검사·원본 크기 확인
실행파일 검사에는 앞 4바이트만 복사하고 Main에서 전달한 Buffer는 추가 전체 복사를 하지 않습니다. 내용이 제공되면 실제 byteLength와 입력 크기가 같아야 합니다. Drive 서비스 회귀 시험에서 위장 PDF가 회사 연결 확인 이후 폴더 생성/파일 전송 요청 없이 거부됨을 확인했습니다. Node 370개가 통과했습니다. 실제 100MiB 전송 메모리·시간 측정은 아직 남아 있습니다.

### 실제 100MiB 바이트 전송 경로 시험
`node desktop-crm/test-rnd-large-file.mjs`는 실제 104,857,600바이트의 파일을 사용하고 Drive API 응답만 모의합니다. multipart Blob의 실제 크기·SHA, 같은 버전 재시도에서 upload POST 1회, 공용 폴더 계보·원본 100MiB 스트림 해시 일치 및 내용 변조 거절을 확인했습니다. rnd-large-file-test-results.txt에 범위·크기·요청 수·소요시간·최종 관측 RSS를 기록합니다. 이 RSS는 fixture 전체 Blob 읽기까지 포함한 한 시점 관측치이며 최대 메모리나 실제 운영 앱 성능이 아닙니다. 실제 회사 클라우드·두 PC 전송은 검수 미완료로 유지합니다.

### 파일명·바이트 입력 경계 확인
공식 업로드는 실제 확장자가 있는 파일명만 허용하며 경로/스트림 구분자·제어/문자 방향 제어·과도한 길이를 거부합니다. 최종 확장자가 실행파일인 이중 확장자는 차단하고 정상 한국어·다중 점 이름은 허용합니다. 내용 검사 입력은 Buffer/Uint8Array/ArrayBuffer로 제한합니다. Node 371개와 실제 100MiB 바이트 모의 전송 회귀 시험이 통과했습니다.

가변 Google 문서 링크는 고정 스냅샷과 구분하여 참고 근거 입력과 Markdown 보고서에 표시합니다. Drive 파일 URL도 회사 보관권한·내용 버전 별도 검증을 안내합니다. source-kind 회귀 시험 포함 Node 372개 통과. 실제 문서 스냅샷 생성은 아직 남아 있습니다.

### 업로드 완료 전 실제 원본 확인
신규 업로드와 기존 버전 재사용은 metadata 확인 이후 회사 보관권한·공용 경로·실제 원본 바이트 SHA/크기를 재검증한 뒤 완료 결과를 반환합니다. 실패에는 Drive 파일 ID를 포함하고 완료 결과·새 복구 기록을 생성하지 않습니다. 실제 100MiB 바이트 모의 API 시험에서 신규·재사용·별도 원본 확인·변조 확인·변조된 기존 버전 재사용 거절까지 5번 스트림 확인 및 upload POST 1회를 확인했습니다. 실제 회사 Drive 전송·고아 파일 원장 복구 검수는 남아 있습니다.

### 업로드됨·원본 검증 미완료 복구 상태
Drive 원본 내용 검증 실패는 알려진 원본 ID·프로젝트·산출물·버전·기대 SHA·크기·관측 시각을 Main에 전달합니다. Main은 해당 시작 계정 원장에 UPLOADED_UNVERIFIED로 보관하고 오류를 반환합니다. 이 기록에는 verifiedAt을 기록하지 않으며 복구 화면에서도 검증 미완료라고 표시합니다. 동일 원본/버전의 재검증이 성공하면 한 기록을 VERIFIED로 갱신하고 과거 검증 완료 기록을 실패 관측으로 자동 낮추지 않습니다. Node 373개와 100MiB 모의 내용 변경 시험이 통과했습니다. 전송 응답 유실·metadata 불일치·모든 고아 파일 복구 경로는 아직 종합 개발/검수가 필요합니다.

### 복구 상태·검증 시각 일관성
복구 원장은 VERIFIED/UPLOADED_UNVERIFIED만 허용하고 정의되지 않은 상태를 자동 완료로 변환하지 않습니다. VERIFIED는 verifiedAt, 미완료는 observedAt이 필요합니다. 같은 원본의 미완료 관측으로 기존 업로드 당시 검증 이력을 덮지 않습니다. 과거 상태 필드가 없는 기록은 유효한 verifiedAt이 있는 경우만 기존 VERIFIED 형식으로 읽습니다. 회귀 시험 포함 Node 374개가 통과했습니다.

### 기준선 기록 포함 백업 v2
메타데이터 백업 v2는 프로젝트와 조회된 기준선 visits를 함께 보관하고 visitsSHA256·visitCount를 검증합니다. 방문 ID/revision·입력값·프로젝트 참조·재방문 원기록 존재를 확인합니다. 회사 CRM 백업 버튼은 기준선 공유 조회도 수행하고 미조회 프로젝트 참조가 있으면 먼저 프로젝트 조회를 안내합니다. 과거 v1 백업은 기준선 미포함으로 읽습니다. 프로젝트 가져오기는 기준선 자동 복원 미지원임을 확인 화면에 표시하며 자료 자체는 백업 파일에 유지합니다. 기준선 신규 초안 복원·공유 저장은 다음 개발 범위입니다. Node 375개 및 별도 백업/Windows 시험을 수행했습니다.

기준선 백업 복원: 기준선 입력 화면의 불러오기에서 검증된 metadata backup을 선택하면 신규 ID만 미공유 초안 목록에 추가합니다. 프로젝트를 먼저 조회하거나 복원해야 하며, 동일 ID의 공유 기록은 유지합니다. 원본 revision을 목록에 표시하고 신규 공유 저장은 revision 0에서 충돌 검사를 거칩니다. 집계에는 공유 기록만 포함됩니다. 복원 초안은 현재 세션 메모리에 있으므로 로그아웃 전 기록별 공유 저장 또는 백업 보관이 필요합니다. 원본 바이너리·권한 복원은 포함하지 않습니다.

기준선 백업·복원 사전 검증은 재방문 원기록 누락, 순환 연결, 건물·프로젝트 불일치를 차단합니다. 동일 ID의 기존 기록을 유지한 최종 복원 목록을 다시 검사합니다. 연결 검사는 반복 방식이며 1만 건 연결 테스트를 통과했습니다. 이는 실제 다중 사용자 성능 시험을 대신하지 않습니다.

복원된 미공유 기준선 목록도 메타데이터 백업에 포함됩니다. 같은 ID가 공유 저장소에 있으면 공유 기록이 우선입니다. 미공유 기록의 revision은 0으로 보관하며, restorationSourceRevision으로 백업에 기재된 원본 버전을 유지합니다. 입력 폼에서 아직 반영·저장하지 않은 추가 편집은 기준선 초안 내보내기를 별도로 사용하세요.

기준선 기록을 열거나 복원하면 원래 projectId를 유지하며 화면에 연결 프로젝트를 표시합니다. 프로젝트 선택 변경으로 기존 기록의 소속을 변경하지 않습니다. 새 기준선 기록 버튼은 새 ID·revision 0과 오늘 날짜로 시작하며 복원 목록은 유지합니다. 기록 전환·새 기록 시작 시 이전 저장/자료 응답의 화면 반영을 차단합니다.

기준선 연결 보호: Main 일반 저장과 운영 repository는 기존 기준선 projectId의 변경·삭제를 원격 쓰기 전에 거부합니다. 연결 이동이 필요하면 별도의 감사 가능한 이관 절차를 구현해야 합니다. 직접 Firebase 요청에 대한 같은 불변 조건의 추가 Rules 검수는 남아 있습니다. CRM IPC 로컬 시험 및 지연 저장 응답 후 새 기준선 revision 유지 시험을 추가했습니다.

DB 기준선 연결 보호: rndControl/visits의 기존 projectId는 일반 DB 쓰기에서도 불변입니다. Emulator에서 프로젝트 변경 거부, 관리자 연결 삭제 거부, 동일 연결 시간 수정 허용 및 전체 기존 Rules 시나리오를 확인했습니다. rules 생성기를 반복 실행한 SHA256도 동일합니다. 운영 Firebase Rules 배포와 실제 계정 검수는 아직 수행하지 않았습니다. 근거: rnd-visit-rules-results.txt, desktop-crm/test-rnd-rules.mjs.

기준선 DB 데이터 검증: 활동 시간 6개·비용 4개는 0 이상의 숫자이어야 하며 활동 합계와 totalMinutes는 0.01 이내로 일치해야 합니다. 건물·담당자·사유·서비스 범위·원가 산정 근거·날짜·원본 링크 필드를 요구하며 빈 문자열과 일반 공백만 있는 값, http/https 이외 링크를 거부합니다. 날짜의 실제 달력 유효성과 재방문 연결 그래프는 앱 검증이 담당하며 추가 서버 검수는 남아 있습니다. Emulator 전체 시험 통과 및 반복 Rules 생성 SHA256 동일 확인. 운영 DB에는 미배포.

공유 저장 응답 유실 복구: 운영 repository는 Main에서 새 UUID operationId를 생성합니다. PATCH 전송·성공 응답 파싱 중 연결 오류가 발생하면 동일 계정으로 최신 기록을 한 번 조회하며, 작업 ID와 전체 저장 내용의 정규화 비교가 일치할 때만 성공을 반환합니다. 불일치·조회 실패는 작업 ID를 포함한 확인 불가 오류로 처리하고 재쓰기는 하지 않습니다. Firebase 빈 배열 생략도 비교에 반영합니다. 프로세스 종료 후 재시도용 영구 작업 원장과 실제 네트워크 장애 시험은 남아 있습니다.

저장 작업 기록(PC별): userData/rnd-save-attempts/{uid}.json에 서버 전송 전 작업 ID·대상·예상 revision·내용 SHA256·시각만 보관합니다. 토큰·본문은 포함하지 않습니다. 계정별 격리, 직렬 원자적 파일 교체, 2MiB·2000건 한도 및 중복 작업 충돌 검증을 적용합니다. 기록 실패는 원격 쓰기를 중단합니다. CRM에서 현재 승인 계정의 최근 50개 요청 기록을 조회할 수 있으며 로그아웃 시 표시를 초기화합니다. 로컬 시험 모드는 운영 원장을 읽지 않습니다. 요청 기록은 저장 완료의 증거가 아니며, 재시작 후 서버와 자동 대조·원장 보관/정리 UI는 후속 구현 대상입니다.

저장 작업 대조: PC에 보관된 현재 계정의 작업을 선택해 현재 공유 기록을 읽기 전용으로 비교할 수 있습니다. 작업 ID·revision·전체 내용 SHA256 일치만 CURRENT_MATCH로 반환합니다. 불일치(CURRENT_DIFFERENT)는 이후 변경 또는 미저장을 구분할 수 없으므로 완료로 확정하지 않습니다. 조회 실패는 READ_FAILED이며 재쓰기·기록 삭제·본문 반환을 하지 않습니다. 각 조회 전후 승인·UID·역할·이메일을 다시 확인하고 세션 변경 후 화면 응답을 폐기합니다. 실제 계정으로 앱 재시작 후 대조, 자동 일괄 대조 및 작업 기록 정리/보관은 추가 검수·개발 대상입니다. UI 대조 표시 시험은 IPC fixture이며 실제 회사 DB 시험을 대신하지 않습니다.

재시작 대조 통합 시험: 실제 임시 디스크 원장에 전송 전 작업을 기록하고, 테스트 서버가 저장 후 응답을 유실하며 즉시 재조회도 실패하는 상황을 구성했습니다. repository·ledger·recovery 서비스를 새로 생성해 같은 디스크 원장을 읽으면 내용 일치를 확인하고, 이후 서버 수정에는 CURRENT_DIFFERENT를 반환합니다. 전체 과정에서 쓰기는 1회입니다. Node 서비스 재생성 시험이며 Windows 프로세스 강제 종료와 실제 회사 서버 장애 검증은 아닙니다. 근거: desktop-crm/test/rnd-save-restart.test.js.

GitHub CI에는 R&D Database Emulator 시험 단계를 추가했습니다. company-site 작업 디렉터리에서 같은 상대 경로·demo project 설정으로 R&D Rules 시험을 로컬 실행해 통과했습니다. CI의 R&D 의존성 설치는 Electron 바이너리 다운로드를 생략합니다. 실제 GitHub 실행 결과는 PR Checks에서 별도로 확인해야 합니다.

GitHub CI 실행 35136744005(ad4806a)에서 Windows desktop 및 backend-and-rules가 모두 통과했습니다. R&D Emulator 시험을 포함한 CI 근거이며 운영 계정 검수를 대신하지 않습니다. 이후 세션 보호 보강은 auth 사용자 객체의 복사본을 보관하고 UID·역할·이메일 변경을 모두 비교합니다. 같은 객체의 내부 이메일 변경도 원격 쓰기 전에 차단하는 모델 시험을 추가했습니다. 새 커밋의 CI 결과는 별도 확인 대상입니다.

복구 계정 보호 보강: 저장 대조와 업로드 복구는 승인 응답 객체를 복사해 초기 UID·역할·이메일을 보관하고 조회·다운로드 이후 현재 승인과 다시 비교합니다. 같은 auth 객체의 내부 이메일/역할 변경으로 초기 비교값까지 바뀌는 경우를 차단합니다. 이메일 변경 후 저장 대조의 원격 조회, viewer 변경 후 업로드 복구의 프로젝트 조회·다운로드가 실행되지 않는 시험을 추가했습니다.

복원 버전 이력: 기준선 복원은 일반 공유 revision과 restorationSourceRevision을 분리합니다. 재백업·재복원·입력 폼의 공유 저장/초안 내보내기에서 기재된 원본 버전을 유지하고 새 기록·로그아웃 시 폼의 이력 값을 초기화합니다. 원본 버전은 백업 기재 정보이며 서버 승인·작성자의 신원을 증명하지 않습니다. 음수·문자열 이력 버전은 앱 검증에서 거부합니다.

복원 버전 DB 검증: restorationSourceRevision은 선택적 필드이며 존재하면 0 이상의 정수여야 합니다. 음수·문자열·소수 직접 DB 요청 거부와 정상 정수 보관 허용을 Emulator 전체 시험에서 확인했습니다. 형식 검증만 수행하며 이 필드 자체로 작성자·원본 승인 여부를 증명하지 않습니다. Rules 생성 반복 SHA256 동일 확인. 운영 DB에는 아직 미배포.

CRM 읽기 전용 context: Main은 기존 fetchRemotePayload의 crmShared/data GET만 호출하며 대기 저장 복구가 가능한 loadStore를 호출하지 않습니다. R&D 승인과 UID·역할·이메일을 조회 전후 확인합니다. 원장 ID·DB key·명칭·건물의 고객 연결 ID·갱신시각만 반환하고 연락처·주소·금액·임의 필드는 제외합니다. 누락 목록은 null/확인 불가로, 명시된 빈 목록은 0건으로 표시합니다. 원장 갱신시각이 없거나 미래이면 확인 필요, 기본 24시간 이상 과거이면 갱신 필요로 표시합니다. CRM 화면에서 최대 20개 원장 ID를 확인할 수 있으며 조회 실패·세션 변경 시 표시를 초기화합니다. 시험 모드 자료를 명시합니다. 실제 회사 원장 검수, 프로젝트별 고정 snapshot 보관·관계 연결·내보내기는 아직 남아 있습니다.

Firebase 배열 빈 자리 처리: CRM context는 배열의 null/undefined 빈 자리만 제외하며 정상 원장 ID를 유지합니다. 값이 존재하는 잘못된 항목은 계속 형식 오류로 처리해 조용히 누락시키지 않습니다. 원본 배열은 변경하지 않습니다. 빈 자리와 정상 2건이 섞인 자료, malformed false 항목 거부의 회귀 시험을 추가했습니다.
시험 캐시 격리 보강: 기본 시험 모드 userData 경로에 PID와 UUID를 사용해 프로세스 ID 재사용 시 다른 시험의 안내 설정·캐시와 겹치지 않도록 했습니다. Windows 시험에서 안내 버튼 대기 실패가 발생한 뒤 확인한 격리 취약 가능성 보강이며, 이전 실패 원인이 캐시였다고 확정한 것은 아닙니다.

CRM context 내용 검증: crm-context-v1 manifest는 원장 ID·건수·조회/갱신시각·시험 모드를 포함한 SHA256을 기록하고 변경을 검사합니다. 화면에서 검증값을 확인할 수 있습니다. checksum은 작성자·원장 출처·서버 승인에 대한 서명이 아니며 프로젝트별 고정 보관은 아직 구현하지 않았습니다. Node 400개, 별도 native 기준선/CRM context 시험 및 Windows packaged 화면 시험 통과.

CI 실행 35138349335(9c935d0)는 기존 FIELD 시험의 전역 Storage.setItem 오류 주입이 Firebase SDK 저장에도 영향을 주어 실패했습니다. 두 자동 저장 시험을 실제 초안 key로 한정하고 실패 key 호출도 확인하도록 수정했습니다. 로컬 FIELD 전체 결과는 44 files 통과, 454 tests 통과·기존 71 skipped이며 FIELD typecheck도 통과했습니다. 이 수정의 GitHub CI는 별도 확인 대상입니다.

### 프로젝트별 CRM 고정 자료
고객·건물 원장 ID 선택과 보관 이유를 입력해 현재 프로젝트에 고정 보관합니다. Main은 CRM 원장을 새로 GET하고 승인·UID·역할·이메일을 재확인합니다. 미공유 프로젝트·동시편집 충돌·누락/과거/갱신시각 미확인 자료·선택 ID 누락·건물 고객 연결 불일치는 거부합니다. 기존 고정 자료를 수정/삭제하지 않고 새 기록을 추가합니다. 일반 프로젝트 저장으로 기록을 새로 추가할 수 없습니다.

기록은 snapshotJSON, 선택 ID JSON, 내용 SHA256, 수집/원장 갱신/보관 시각, 보관자 UID, 당시 프로젝트 revision을 보관합니다. JSON 문자열로 Firebase의 빈 배열 생략을 피합니다. 내용 checksum은 작성자·원장 출처·승인 여부의 인증 서명이 아닙니다. Main 수집 경로를 우회하는 직접 DB 요청의 JSON 내부 내용과 SHA256을 Rules만으로 확인할 수는 없으며 이 출처 신뢰 보강은 추가 개발 범위입니다.

DB Rules는 승인된 쓰기 역할의 새 기록만 허용하고 보관자 UID·프로젝트 ID·source revision·현재 CRM 갱신시각·보관 epoch 시각·SHA 형식·허용 필드를 확인합니다. 시험 모드 자료는 운영 DB에 저장하지 못합니다. 관리자도 기존 기록 수정·삭제가 거부됩니다. Emulator에서 이 거부, JSON 내용 유지 및 동시 두 기록의 단일 revision 승자/이전 기록 보존을 확인했습니다. 운영 Rules는 미배포입니다.

고정 자료는 내부 Markdown 묶음에 원장 ID·관계·보관 이유·수집 자료와 함께 포함됩니다. 외부 검토용 내보내기는 전용 공개 범위 승인이 구현될 때까지 모든 CRM 고정 자료를 제외합니다. 메타데이터 백업에는 자료가 유지되지만 신규 공유 프로젝트로 이 기록까지 복구하려면 별도의 승인된 복원 작업을 추가해야 합니다.

화면 저장 대기 중 바뀐 프로젝트 편집은 덮지 않습니다. 최신 공유본과 비교·선택 재적용 후 일반 저장하면 새 CRM 고정 자료를 유지합니다. 로그아웃/세션 초기화 후 지연 응답으로 이전 프로젝트를 복원하지 않습니다. 실제 Main 저장과 공유 조회를 포함한 native 시험은 명시적 시험 고객/건물 fixture로 수행했으며 회사 원장 검수가 아닙니다. 화면 준비 직후 연결을 전달해 첫 1.5초 안의 저장 연결 누락도 수정했습니다.

Node 410개와 신규 CRM 고정 자료 native·Windows packaged 시험, 전체 native·packaged R&D 화면/비활성화 시험, 전체 R&D Rules Emulator 시험이 통과했습니다. GitHub CI 35138938715(8b7ded9)는 Windows desktop 및 backend-and-rules 모두 통과했습니다. 이번 고정 자료 커밋의 CI·실제 회사 원장/다중 PC 검수는 별도 확인 대상입니다.

CRM 고정 자료 코드 리뷰에서 지연 응답 후 미제출 산출물 입력·blur 전 제목이 덮이는 문제를 발견하고 수정했습니다. 현재 프로젝트의 CRM 보관 폼 외 미반영 입력이 있으면 최신 공유본 수용/화면 재렌더를 미루고 입력을 유지합니다. 독립 리뷰 재확인에서 해당 Important 항목은 해소되었으며 별도 Critical/Important 항목은 없었습니다. 실제 Main 생성 내부/검토용 ZIP을 Python으로 검사해 CRM 자료 제외·CRC·UTF-8·파일 크기/SHA256·모든 상대 링크를 확인했습니다. 근거: crm-context-review.md 및 native/packaged CRM 고정 시험 로그.

GitHub 코드 경로 검증 보강: SHA가 있어도 blob 파일 경로 누락·경로 이동·인코딩된 구분자/제어문자·잘못된 escape·commit 뒤 추가 경로가 있으면 등록을 거부합니다. Node 411개 통과. 인증된 읽기 전용 GitHub API로 a813b0d 커밋 존재와 Main/CRM context/code source 파일 3개의 원격 blob 바이트가 같은 로컬 커밋과 일치함을 확인했습니다. github-source-verification.json은 해당 커밋/파일/관측 시각에 한정된 근거이며 일반 등록 자료의 자동 원격 검증이나 연구 주장의 타당성을 증명하지 않습니다.

GitHub CI 35140606062(a813b0d)는 프로젝트별 CRM 고정 자료·DB 불변/동시 저장 시험을 포함해 Windows desktop과 backend-and-rules 모두 통과했습니다. 후속 코드 경로 검사 커밋은 별도 CI 확인 대상입니다.

후속 실행 업무 개발 착수: follow-up.mjs에 결정 연결·담당/검토자·기한·완료 기준·불변 상태 이벤트의 domain 처리 로직을 추가했습니다. 담당자 진행/검토 요청과 지정 검토자 완료 확정, 자기검토 이유, 결과 자료 링크, stale/중복 이벤트 거부를 시험했습니다. Node 414개 통과. 현재 domain 기반만 구현했으며 Main 공유 저장·서버 task head 제약·UI·보고서 연결은 계획에 따라 이어서 구현해야 합니다. 기존 Windows 검토본에는 이 후속 업무 화면이 아직 없습니다. 개발 계획: docs/superpowers/plans/2026-09-17-decision-follow-up.md. 코드 경로 검사 21d0710의 CI 35140917469는 desktop 및 backend-and-rules 모두 통과했습니다.

후속 업무 통합 갱신: Main·원자적 이벤트/task head·Rules·CRM 생성/진행/검토/완료·프로젝트별 입력 초안·지연 응답·검색/포트폴리오·내부 Markdown 이력을 연결했습니다. Node421 및 focused native 시험 통과. 실제 Main ZIP의 CRC/해시/UTF8와 검토용 제외 확인. 기존 Windows 검토본 갱신 및 packaged 후속 업무 검수는 아직 미완료입니다. decision-follow-up-integration.md, follow-up-ui-review.md에 상세 범위를 기록했습니다.

Windows 후속 업무 검토본 갱신: dcbdfe6 소스로 x64 디렉터리 빌드를 만들고 packaged 후속 업무/기존 CRM·R&D/CRM 고정 보관/비활성화 시험을 모두 통과했습니다. ASAR의 src75개가 해당 커밋 소스와 바이트 단위 일치합니다. 전달 ZIP에는 해시 목록과 packaged 시험 로그를 포함합니다. 설치·운영 배포·회사 실계정/Drive/두 PC 검수는 완료되지 않았습니다.

권한 회수 후 업무 정리 보완: 신규 배정 시 승인 CRM/R&D 담당·검토자만 허용하고, 기존 업무의 각 변경은 현재 행위자 권한을 검증합니다. 담당자의 권한이 회수된 뒤에도 관리자는 사유를 남겨 취소할 수 있으며 기존 담당/이력을 바꾸지 않습니다. 회수 계정의 쓰기 및 새 배정은 거부됩니다. 전체 Rules emulator 시험 통과; 운영 Rules는 배포하지 않았습니다.

관측 기록 통합 갱신: 실제 0값과 결측·미점검·관측 불가를 구분하는 입력, 프로젝트별 초안, 검색·상태별 집계·내부 보고서를 연결했습니다. Node423, 전체 emulator 및 native/packaged Main·UI·ZIP 검수 통과. Windows 소스70e2297의 src77개가 ASAR와 바이트 단위 일치합니다. EXP-04의 로컬 자동 검수 근거를 기록했으며 회사 운영 배포·실계정/Drive 검수는 미완료입니다. observation-status.md에 검수 범위를 기록했습니다.

복원 사전 안내 보완: JSON 백업의 프로젝트별 ID·이름·현재/백업 revision을 확인하고 같은 ID는 유지하며 신규 항목만 초안으로 추가합니다. Node424 및 native/packaged 취소·적용 시험 통과. Windows 소스 d008270, src77 바이트 일치 및 관측/후속업무/기존 CRM/고정 보관/비활성화 검수 통과. 대량 목록 검토·CSV 전체 import·승인 복원·rollback manifest와 운영 검수는 남아 있습니다.

대량 복원 검토 화면: 50개씩 페이지 이동하고 충돌·신규 항목을 필터로 확인합니다. 검토 중 프로젝트 수정 또는 세션 변경 시 적용을 중단합니다. native/packaged101건 목록과 원본 유지·초안 추가·취소 검수 통과. Windows 소스48cca6d의 src78개가 ASAR와 바이트 단위 일치합니다. CRM-04 전체 범위의 CSV·승인 복원·rollback manifest와 회사 실환경 검수는 남아 있습니다.

Windows 검토본 최종 갱신: e8990bb 소스의 복원 검토 화면과 해시 오인 수정까지 포함했습니다. Node426, packaged 복원/관측/후속 업무/기존 CRM/자료 고정 보관/비활성화 시험 통과. src78개 ASAR 바이트 일치와 소스 커밋·해시 근거를 포함합니다. 일반 메모·비밀번호·JSON 숫자/키/중복 항목의 민감정보 차단은 유지합니다. 상세 검수: sensitive-hash-review.md. 운영 배포와 회사 실환경 검수는 미완료입니다.

복원 명세 검토본 갱신: 복원 전/입력 자료·충돌 계획·적용 후 프로젝트 해시를 기록하고 다운로드합니다. Node428 및 native/packaged 실제 파일 읽기·해시·원본 유지·지연 계산 중 변경 거부·세션 정리 시험 통과. Windows 소스51dfc76의 src79개가 ASAR와 일치합니다. 명세는 로컬 프로젝트 메타데이터이며 방문/원본 파일/공유 DB·Drive 백업은 포함하지 않습니다. 되돌리기 실행 화면과 승인된 공유 복원은 남아 있습니다. 상세 근거: restore-manifest.md.

복원 되돌리기 검토본 갱신: 최신 세션의 명세를 기준으로 추가된 로컬 초안만 정리합니다. native/packaged101건 페이지·취소·확인 후 지연 계산 중 새 입력 거부·검토 중 세션 초기화 검수 통과. 기존 dirty 상태와 Main 공유 원본을 유지합니다. Windows 소스0a8c465, src80개 ASAR 일치 및 기존 CRM/관측/후속 업무/고정 보관/비활성화 검수 통과. 공유 DB 복원과 일반 명세 재실행·회사 Drive 실연동은 남아 있습니다. 상세 근거: restore-manifest.md.
