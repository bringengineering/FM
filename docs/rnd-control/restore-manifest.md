# Local draft restoration and rollback
Project restoration generates a metadata-only manifest with before/source snapshots, selected project, exact conflict plan, expected-after SHA inventory and integrity digest. Download is requested after application and retained for repeat download during the session. It does not include visits/binaries or provide Drive/shared-database backup or authenticated authorship.
Reader recomputes structure, relationships, plan and hashes. Latest-session rollback reviews imported addition IDs/titles with50-row pages and verifies the full current project inventory matches expected-after. It rejects changed/saved additions, unsent forms/hidden drafts, baseline drafts, saves/uploads and pending workflows. Guards repeat after asynchronous validation and confirmation. Existing dirty flags/prior selection and shared original are preserved; retained manifest remains downloadable. Reset cancels review and clears pending controls.
Verification: Node428, native and x64 packaged restore/rollback/download checks pass. Rollback101 rows traverse50/50/1 without gaps/duplicates; cancel retains102 projects. New input during post-confirm digest prevents removal, and reset during review preserves actual Main shared original. Saved-addition refusal, delayed project-change refusal and unsent input preservation also pass. Independent implementation/test review reports no Critical/Important issue.
Windows source0a8c465:80 source files byte-identical to ASAR. Packaged observation/follow-up/baseline/CRM context/disabled checks all pass. Refreshed delivery archives include source binding and runtime evidence.
CRM-04 remains incomplete for whole CSV import, uploaded-manifest replay, approved immutable-history/shared restoration and actual company operational rollback. Local fixture results do not prove real company Drive/2PC operation.
