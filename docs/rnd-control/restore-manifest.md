# Restore manifest checkpoint
Local project draft restoration now generates metadata-only before/source/after manifest with an exact conflict plan, selected project, SHA inventory and integrity digest. Automatic browser download is requested after application; the last manifest is available for repeat download and cleared on session reset. This is not Drive persistence or an authenticated signature, and does not include visits or file binaries.
Reader recomputes schema, references, plan and expected-after hashes. Pure rollback helper returns before snapshots only if every current project and hash still matches the exact after inventory; newer changes/additions are rejected. There is no rollback UI or shared-database deletion/replacement.
Verification: Node428; native actual-download file read validates bytes/hash/original/conflicts. Delayed hash cannot apply after project change; cancel/stale/reset and101-record review pass. Independent scope review reports no Critical/Important issue. Packaged and delivery refresh pending. CRM-04 remains incomplete for CSV whole import, approved immutable-history/shared restoration and operational rollback.
