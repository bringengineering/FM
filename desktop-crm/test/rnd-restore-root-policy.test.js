const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');
test('root restore read is restricted to approved CRM admin with matching R&D approval and root writes stay denied',()=>{const rules=JSON.parse(fs.readFileSync(path.join(__dirname,'../../database.rules.json'),'utf8')).rules;const root=rules.rndControl;assert.equal(typeof root['.read'],'string');for(const field of ['crmCompany/access','rndAccess','enabled','email','role','admin','auth.token.email'])assert.ok(root['.read'].includes(field));assert.equal(root['.write'],false);});
