const test=require('node:test');const assert=require('node:assert/strict');
test('improvement distinguishes missing values, zero baseline and measured zero result',async()=>{const {calculateImprovement}=await import('../src/rnd-control/evidence-quality.mjs');assert.equal(calculateImprovement(null,5).percent,null);assert.equal(calculateImprovement(0,5).percent,null);assert.equal(calculateImprovement(10,0).percent,100);assert.equal(calculateImprovement(10,12).percent,-20);assert.equal(calculateImprovement(Infinity,5).percent,null);assert.equal(calculateImprovement(Number.MIN_VALUE,Number.MAX_VALUE).percent,null);});
test('measured evidence requires frozen data and source confirmation; reference evidence remains non-measured',async()=>{const {applyResearch}=await import('../src/rnd-control/research.mjs');const p={id:'p',research:{experiments:[{id:'exp',status:'closed',dataset:{version:'v1',url:'https://example.com/data',sampleUnits:3}}]}};const c={type:'evidence',id:'e',experimentId:'exp',claim:'개선',limitations:'3동',reviewerUid:'r',evidenceType:'measured',sourceDescription:'BRING 현장 측정',measurementConfirmed:true};assert.throws(()=>applyResearch(p,c,{uid:'u',role:'member'}),/동결/);const q=applyResearch(p,{type:'referenceEvidence',id:'lit',claim:'선행 연구 수치',limitations:'BRING 실측 아님',reviewerUid:'r',evidenceType:'literature',sourceDescription:'선행 논문',url:'https://example.com/paper',version:'2026-1'},{uid:'u',role:'member'});assert.equal(q.research.evidence[0].evidenceType,'literature');assert.equal(q.research.evidence[0].source.sampleUnits,0);});
