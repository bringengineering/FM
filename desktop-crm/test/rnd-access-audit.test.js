const test=require('node:test');const assert=require('node:assert/strict');test('approval audit preserves previous and proposed state with explicit attempt semantics',()=>{const {createAccessAudit}=require('../src/rnd-control/access-audit');const before={enabled:true,email:'m@test.invalid',role:'member',approvedBy:'old'};const after={enabled:false,email:before.email,role:before.role,approvedBy:'admin',approvedAt:'2026-09-17T00:00:00Z',reason:'revoked'};const event=createAccessAudit({id:'event',uid:'m',before,after,expectedETag:'"v1"',actor:{uid:'admin',role:'admin'}});before.enabled=false;assert.equal(event.before.enabled,true);assert.equal(event.after.enabled,false);assert.equal(event.kind,'approval-attempt');assert.equal(event.committed,false);assert.throws(()=>createAccessAudit({id:'event',uid:'m',before,after,actor:{uid:'member',role:'member'}}));});
