export function mountCodeVerification({form,api}){if(!form||!api?.rndVerifyCodeSource)return;let epoch=0;const button=document.createElement('button'),status=document.createElement('p');button.type='button';button.id='rndCodeSourceVerify';button.textContent='고정 GitHub 파일 원본 확인';status.id='rndCodeSourceVerification';status.setAttribute('role','status');form.append(button,status);const reset=()=>{epoch++;status.textContent='';button.disabled=false;};form.addEventListener('input',reset);window.addEventListener('rnd-session-reset',reset);let lastProject=window.BringRndProject?.current()?.id;window.addEventListener('rnd-project-selected',event=>{if(event.detail?.id!==lastProject){lastProject=event.detail?.id;reset();}});button.onclick=async()=>{const version=++epoch,projectId=window.BringRndProject?.current()?.id,input={url:form.elements.url.value,version:form.elements.version.value};button.disabled=true;status.textContent='고정 GitHub 파일 확인 중…';const current=()=>version===epoch&&form.isConnected&&window.BringRndProject?.current()?.id===projectId;try{const result=await api.rndVerifyCodeSource(input);if(!current())return;status.textContent=result.status==='VERIFIED'?`파일 원본 확인 · ${result.repository}/${result.path} · commit ${result.commitSHA} · ${result.sizeBytes}bytes · SHA-256 ${result.sha256} · 파일 내용 확인이며 기술 주장 승인은 별도입니다.`:'파일 접근 또는 존재를 확인하지 못했습니다. 비공개 저장소 권한과 경로를 확인하세요. 삭제를 확정한 상태가 아닙니다.';}catch(error){if(current())status.textContent=`파일 확인 중단 · ${error.message}`;}finally{if(version===epoch)button.disabled=false;}};}
