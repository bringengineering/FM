export const evidenceLabels={unclassified:'미분류',measured:'BRING 실측',literature:'문헌',simulation:'시뮬레이션',assumption:'가정'};
export function calculateImprovement(baseline,result,direction='lower_better'){if(typeof baseline!=='number'||typeof result!=='number'||!Number.isFinite(baseline)||!Number.isFinite(result))return{percent:null,reason:'미측정 또는 유효한 수치 아님'};if(baseline<=0||result<0)return{percent:null,reason:'양수 기준값과 0 이상 결과값 필요'};if(!['lower_better','higher_better'].includes(direction))return{percent:null,reason:'개선 방향 미확정'};const percent=(direction==='lower_better'?baseline-result:result-baseline)/baseline*100;return Number.isFinite(percent)?{percent,reason:null}:{percent:null,reason:'계산 범위 초과'};}
export function evidenceQuality(command,hasFrozenData=false){const evidenceType=command.evidenceType??'unclassified';if(!Object.hasOwn(evidenceLabels,evidenceType))throw Error('근거 유형 오류');if(evidenceType!=='unclassified'&&!command.sourceDescription?.trim())throw Error('근거 출처와 적용 범위를 기록하세요');if(evidenceType==='measured'&&(!hasFrozenData||command.measurementConfirmed!==true))throw Error('실측 근거에는 동결 자료와 BRING 직접 측정 확인이 필요합니다');return{evidenceType,sourceDescription:command.sourceDescription?.trim()??'',measurementConfirmed:evidenceType==='measured'&&command.measurementConfirmed===true};}
