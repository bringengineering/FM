export function askRndText(label){return new Promise(resolve=>{const dialog=document.createElement('dialog');dialog.className='rnd-text-dialog';const form=document.createElement('form');form.method='dialog';const title=document.createElement('h3');title.textContent=label;const wrapper=document.createElement('label');wrapper.textContent=label;const input=document.createElement('input');input.name='answer';input.autocomplete='off';wrapper.append(input);const actions=document.createElement('div');actions.className='actions';const cancel=document.createElement('button');cancel.type='button';cancel.textContent='취소';cancel.onclick=()=>dialog.close('cancel');const submit=document.createElement('button');submit.textContent='확인';actions.append(cancel,submit);form.append(title,wrapper,actions);dialog.append(form);document.body.append(dialog);form.onsubmit=e=>{e.preventDefault();dialog.close('confirm');};dialog.addEventListener('close',()=>{const answer=dialog.returnValue==='confirm'?input.value:null;dialog.remove();resolve(answer);},{once:true});dialog.showModal();input.focus();});}
