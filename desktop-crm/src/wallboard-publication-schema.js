const invalid=()=>{throw Object.assign(new Error('INVALID_INPUT'),{code:'INVALID_INPUT'});};
const shape=(value,keys)=>{if(!value||typeof value!=='object'||Array.isArray(value)||Object.keys(value).length!==keys.length||keys.some(k=>!Object.hasOwn(value,k)))invalid();};
const count=n=>{if(!Number.isSafeInteger(n)||n<0||n>1000000)invalid();};
const pct=n=>{if(!Number.isInteger(n)||n<0||n>100)invalid();};
const finite=(n,min,max)=>{if(typeof n!=='number'||!Number.isFinite(n)||n<min||n>max)invalid();};
const text=(s,max)=>{if(typeof s!=='string'||s.length>max||/[\u0000-\u0008\u000b-\u001f]/.test(s))invalid();};
const date=value=>{if(typeof value!=='string'||!/^\d{4}-\d{2}-\d{2}$/.test(value)||!Number.isFinite(Date.parse(value))||new Date(value+'T00:00:00Z').toISOString().slice(0,10)!==value)invalid();};
const optionalDate=value=>{if(value!=='')date(value);};
const health=value=>{if(!['normal','check','risk','done'].includes(value))invalid();};
function scheduleEntry(entry){shape(entry,['date','time','endTime','title','owner','status']);date(entry.date);if(!(entry.time==='시간 미정'||/^([01]\d|2[0-3]):[0-5]\d$/.test(entry.time)))invalid();if(!(entry.endTime===''||/^([01]\d|2[0-3]):[0-5]\d$/.test(entry.endTime)))invalid();text(entry.title,80);text(entry.owner,40);if(!['예정','진행 중','완료','상태 확인 필요'].includes(entry.status))invalid();}
function validatePublication(input){
 shape(input,['model','playlist','notice','dataDate']);text(input.notice,160);date(input.dataDate);
 const m=input.model;shape(m,['counts','total','overdue','unknown','people','schedule','roadmap','portfolio']);
 const states=['assigned','doing','submitted','returned','done'];shape(m.counts,states);states.forEach(s=>count(m.counts[s]));['total','overdue','unknown'].forEach(k=>count(m[k]));
 if(states.reduce((sum,k)=>sum+m.counts[k],0)!==m.total||m.overdue>m.total-m.counts.done)invalid();
 if(!Array.isArray(m.people)||m.people.length>100)invalid();for(const p of m.people){shape(p,['name','total','done','overdue']);text(p.name,40);['total','done','overdue'].forEach(k=>count(p[k]));if(p.done+p.overdue>p.total)invalid();}
 if(m.people.reduce((n,p)=>n+p.total,0)!==m.total||m.people.reduce((n,p)=>n+p.done,0)!==m.counts.done||m.people.reduce((n,p)=>n+p.overdue,0)!==m.overdue)invalid();
 shape(m.schedule,['available','entries','today','week']);if(typeof m.schedule.available!=='boolean')invalid();for(const key of ['entries','today','week'])if(!Array.isArray(m.schedule[key])||m.schedule[key].length>200)invalid();
 if(!m.schedule.available&&(m.schedule.entries.length||m.schedule.today.length||m.schedule.week.length))invalid();for(const e of m.schedule.entries){shape(e,['time','status']);if(!['예정','진행 중','완료','상태 확인 필요'].includes(e.status)||!(e.time==='시간 미정'||/^([01]\d|2[0-3]):[0-5]\d$/.test(e.time)))invalid();}m.schedule.today.forEach(scheduleEntry);m.schedule.week.forEach(scheduleEntry);
 shape(m.roadmap,['range','lanes']);shape(m.roadmap.range,['from','to','todayOffset','weeks']);date(m.roadmap.range.from);date(m.roadmap.range.to);finite(m.roadmap.range.todayOffset,0,100);if(!Array.isArray(m.roadmap.range.weeks)||m.roadmap.range.weeks.length>8)invalid();for(const week of m.roadmap.range.weeks){shape(week,['start','end','label']);date(week.start);date(week.end);text(week.label,20);}
 if(!Array.isArray(m.roadmap.lanes)||m.roadmap.lanes.length>100)invalid();for(const lane of m.roadmap.lanes){shape(lane,['assigneeName','assignments','projectCount','progress']);text(lane.assigneeName,40);count(lane.projectCount);pct(lane.progress);if(!Array.isArray(lane.assignments)||lane.assignments.length>100||lane.projectCount!==lane.assignments.length)invalid();for(const item of lane.assignments){shape(item,['projectName','startDate','endDate','progress','health','layout']);text(item.projectName,120);optionalDate(item.startDate);optionalDate(item.endDate);pct(item.progress);health(item.health);if(item.layout!==null){shape(item.layout,['left','width','clippedStart','clippedEnd']);finite(item.layout.left,0,100);finite(item.layout.width,0,100);if(typeof item.layout.clippedStart!=='boolean'||typeof item.layout.clippedEnd!=='boolean')invalid();}}}
 shape(m.portfolio,['overallProgress','healthCounts','projects','weeklyDone','milestones']);pct(m.portfolio.overallProgress);shape(m.portfolio.healthCounts,['normal','check','risk','done']);Object.values(m.portfolio.healthCounts).forEach(count);
 if(!Array.isArray(m.portfolio.projects)||m.portfolio.projects.length>100)invalid();for(const p of m.portfolio.projects){const reviewFields=Object.hasOwn(p||{},'reviewedDone')||Object.hasOwn(p||{},'reviewedTotal');shape(p,reviewFields?['name','owner','progress','health','open','reviewedDone','reviewedTotal']:['name','owner','progress','health','open']);text(p.name,120);text(p.owner,80);pct(p.progress);health(p.health);count(p.open);if(reviewFields){count(p.reviewedDone);count(p.reviewedTotal);if(p.reviewedDone>p.reviewedTotal)invalid();}}
 if(!Array.isArray(m.portfolio.weeklyDone)||m.portfolio.weeklyDone.length>8)invalid();for(const row of m.portfolio.weeklyDone){shape(row,['label','count']);text(row.label,20);count(row.count);}
 if(!Array.isArray(m.portfolio.milestones)||m.portfolio.milestones.length>6)invalid();for(const row of m.portfolio.milestones){shape(row,['title','projectName','owner','dueDate','daysLeft']);text(row.title,80);text(row.projectName,120);text(row.owner,40);date(row.dueDate);if(!Number.isInteger(row.daysLeft)||row.daysLeft<-3650||row.daysLeft>3650)invalid();}
 if(!Array.isArray(input.playlist)||input.playlist.length>10)invalid();const seen=new Set(),keys=new Set(['roadmap','portfolio','weeklyTrend','health','milestones','scheduleToday','scheduleWeek','people','issues','notice']);for(const p of input.playlist){shape(p,['key','enabled','seconds']);if(!keys.has(p.key)||seen.has(p.key)||typeof p.enabled!=='boolean'||!Number.isInteger(p.seconds)||p.seconds<10||p.seconds>120)invalid();seen.add(p.key);}
 return structuredClone(input);
}
module.exports={validatePublication};
