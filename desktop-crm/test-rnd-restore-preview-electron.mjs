import {_electron as electron} from 'playwright';
import assert from 'node:assert/strict';
import {createRequire} from 'node:module';
import {dirname,join,resolve,sep} from 'node:path';
import {mkdtemp,readFile,rm} from 'node:fs/promises';
import {tmpdir} from 'node:os';
import {readRestoreManifest} from './src/rnd-control/restore-manifest.mjs';
import {fileURLToPath} from 'node:url';
import {createMetadataBackup} from './src/rnd-control/backup.mjs';
const require=createRequire(import.meta.url);
const app=await electron.launch({executablePath:process.env.BRING_RND_PACKAGED_EXE||require('electron'),args:process.env.BRING_RND_PACKAGED_EXE?[]:['.'],cwd:dirname(fileURLToPath(import.meta.url)),env:{...process.env,BRING_CRM_LOCAL_ONLY:'1',BRING_CRM_SCREENSHOT_ROLE:'admin'}});
const exportDir=await mkdtemp(join(tmpdir(),'bring-restore-manifest-'));
try{
 await app.evaluate(({session},path)=>session.defaultSession.on('will-download',(_event,item)=>{item.setSavePath(path);item.once('done',(_event,state)=>{globalThis.restoreDownloadState=state;});}),join(exportDir,'manifest.json'));
 const page=await app.firstWindow();await page.waitForSelector('[data-view=rndControl]');await page.locator('[data-action=finish-guide]').click();await page.locator('[data-view=rndControl]').click();await page.waitForFunction(()=>document.getElementById('login').hidden);
 await page.locator('#newProject').click();await page.locator('.rnd-text-dialog input').fill('Current restore project');await page.locator('.rnd-text-dialog').getByRole('button',{name:'확인',exact:true}).click();await page.locator('#portfolioSave').click();await page.waitForFunction(()=>document.getElementById('portfolioStatus').textContent.includes('공유 저장 완료'));
 await page.locator('#projectTitle').fill('Current restore project draft');await page.locator('#projectTitle').dispatchEvent('change');assert.equal(await page.evaluate(()=>window.BringRndProject.hasUnsavedChanges()),true);
 const current=await page.evaluate(()=>window.BringRndProject.current());const incoming=structuredClone(current);incoming.title='Backup conflict title';const addition=structuredClone(current);addition.id='restore-preview-addition';addition.title='New restored draft';addition.revision=0;delete addition.research;const backup=await createMetadataBackup([incoming,addition],incoming.id);const payload={name:'backup.json',mimeType:'application/json',buffer:Buffer.from(JSON.stringify(backup))};
 await page.locator('#archiveImport').setInputFiles(payload);await page.locator('#rndRestoreReview').waitFor();const message=await page.locator('#rndRestoreReview').textContent();assert.ok(message.includes(current.id));assert.ok(message.includes('Backup conflict title'));assert.ok(message.includes('Current restore project'));assert.ok(message.includes('restore-preview-addition'));await page.locator('#rndRestoreCancel').click();await page.waitForFunction(()=>document.getElementById('archiveImport').value==='');assert.equal(await page.locator('#projectPicker option[value="restore-preview-addition"]').count(),0);
 await page.locator('#archiveImport').setInputFiles(payload);await page.locator('#rndRestoreApply').click();await page.locator('#projectPicker option[value="restore-preview-addition"]').waitFor({state:'attached'});assert.equal(await page.evaluate(()=>window.BringRndProject.current().title),current.title);await assert.rejects(()=>page.evaluate(id=>window.bringCRM.rndGet('projects',id),addition.id),/먼저 공유 저장/);console.log('PASS native restore review cancel/no-change, apply draft-only and existing project retained');await app.evaluate(async()=>{for(let i=0;i<60&&!globalThis.restoreDownloadState;i++)await new Promise(r=>setTimeout(r,100));if(globalThis.restoreDownloadState!=='completed')throw Error('Restore manifest download failed');});const downloaded=await readRestoreManifest(await readFile(join(exportDir,'manifest.json'),'utf8'));assert.equal(downloaded.beforeProjects[0].title,current.title);assert.deepEqual(downloaded.plan.additions,[addition.id]);assert.deepEqual(downloaded.plan.conflicts,[current.id]);assert.equal(downloaded.cloudWrites,false);assert.equal(await page.locator('#rndRestoreManifestDownload').isDisabled(),false);console.log('PASS native downloaded restore manifest bytes/hash/plan and original metadata');
 await page.locator('#rndRestoreRollback').click();await page.locator('#rndRollbackReview').waitFor();assert.ok((await page.locator('#rndRollbackRows').textContent()).includes(addition.id));await page.locator('#rndRollbackCancel').click();assert.equal(await page.locator('#projectPicker option[value="restore-preview-addition"]').count(),1);await page.locator('#rndRestoreRollback').click();await page.locator('#rndRollbackApply').click();await page.waitForFunction(()=>document.getElementById('portfolioStatus').textContent.includes('되돌리기 완료'));assert.equal(await page.locator('#projectPicker option[value="restore-preview-addition"]').count(),0);assert.equal(await page.evaluate(()=>window.BringRndProject.current().title),current.title);assert.equal(await page.evaluate(id=>window.bringCRM.rndGet('projects',id),current.id).then(p=>p.revision),current.revision);assert.equal(await page.locator('#rndRestoreManifestDownload').isDisabled(),false);assert.equal(await page.locator('#rndRestoreRollback').isDisabled(),true);assert.equal(await page.evaluate(()=>window.BringRndProject.hasUnsavedChanges()),true);console.log('PASS native rollback cancel/execute removes only restored local addition and preserves shared original');
 await page.locator('#archiveImport').setInputFiles(payload);await page.locator('#rndRestoreApply').click();await page.locator('#projectPicker option[value="restore-preview-addition"]').waitFor({state:'attached'});await page.locator('#itemForm [name=goal]').fill('Unsent before rollback');await page.locator('#rndRestoreRollback').click();await page.waitForFunction(()=>document.getElementById('portfolioStatus').textContent.includes('대기'));assert.equal(await page.locator('#rndRollbackReview').count(),0);assert.equal(await page.locator('#itemForm [name=goal]').inputValue(),'Unsent before rollback');await page.evaluate(()=>{document.getElementById('itemForm').reset();window.BringRndClearInput('itemForm');});console.log('PASS native rollback refuses unsent input without clearing it');
 await page.evaluate(()=>{const original=crypto.subtle.digest.bind(crypto.subtle);let held=false;crypto.subtle.digest=async(...args)=>{const out=await original(...args);if(!held){held=true;await new Promise(resolve=>{window.releaseRollbackDigest=()=>{delete window.releaseRollbackDigest;crypto.subtle.digest=original;resolve();};});}return out;};});await page.locator('#rndRestoreRollback').click();await page.waitForFunction(()=>!!window.releaseRollbackDigest);assert.equal(await page.evaluate(()=>window.BringRndRollbackPending()),true);await page.evaluate(()=>{const p=window.BringRndProject.current();p.title='Changed during rollback hash';window.dispatchEvent(new CustomEvent('rnd-project-saved',{detail:p}));window.releaseRollbackDigest();});await page.waitForFunction(()=>!window.BringRndRollbackPending());assert.equal(await page.locator('#rndRollbackReview').count(),0);assert.equal(await page.locator('#projectPicker option[value="restore-preview-addition"]').count(),1);assert.equal(await page.evaluate(()=>window.BringRndProject.current().title),'Changed during rollback hash');console.log('PASS delayed rollback validation cannot remove drafts after newer project change');
 await page.locator('#projectPicker').selectOption(addition.id);await page.locator('#portfolioSave').click();await page.waitForFunction(()=>document.getElementById('portfolioStatus').textContent.includes('공유 저장 완료'));const savedAddition=await page.evaluate(id=>window.bringCRM.rndGet('projects',id),addition.id);await page.locator('#rndRestoreRollback').click();await page.waitForFunction(()=>document.getElementById('portfolioStatus').textContent.includes('공유 저장됐거나'));assert.equal(await page.locator('#rndRollbackReview').count(),0);assert.equal(await page.evaluate(id=>window.bringCRM.rndGet('projects',id),addition.id).then(p=>p.revision),savedAddition.revision);console.log('PASS saved restored project cannot be removed by local draft rollback');
 const many=Array.from({length:100},(_,i)=>({...structuredClone(addition),id:'review-'+i,title:i===0?'<img src=x onerror=alert(1)>':'Review '+i}));const large=await createMetadataBackup([incoming,...many],incoming.id);const largePayload={name:'large.json',mimeType:'application/json',buffer:Buffer.from(JSON.stringify(large))};await page.locator('#archiveImport').setInputFiles(largePayload);await page.locator('#rndRestoreReview').waitFor();const seen=[];for(const count of [50,50,1]){assert.equal(await page.locator('#rndRestoreRows article').count(),count);seen.push(...await page.locator('#rndRestoreRows article').allTextContents());if(count===50)await page.locator('#rndRestoreNext').click();}assert.equal(new Set(seen).size,101);assert.equal(await page.locator('#rndRestoreNext').isDisabled(),true);await page.locator('#rndRestoreFilter').selectOption('KEEP_EXISTING');assert.equal(await page.locator('#rndRestoreRows article').count(),1);assert.ok((await page.locator('#rndRestoreRows').textContent()).includes(current.id));await page.locator('#rndRestoreFilter').selectOption('ADD_DRAFT');assert.equal(await page.locator('#rndRestoreRows article').count(),50);assert.ok((await page.locator('#rndRestoreRows').textContent()).includes('<img src=x onerror=alert(1)>'));assert.equal(await page.locator('#rndRestoreRows img').count(),0);await page.locator('#rndRestoreCancel').click();await page.waitForFunction(()=>document.getElementById('archiveImport').value==='');console.log('PASS native restore101-row paging, conflict/addition filters and literal markup');
 await page.locator('#archiveImport').setInputFiles(largePayload);await page.locator('#rndRestoreReview').waitFor();await page.evaluate(()=>{const original=crypto.subtle.digest.bind(crypto.subtle);let held=false;crypto.subtle.digest=async(...args)=>{const out=await original(...args);if(!held){held=true;await new Promise(resolve=>{window.releaseRestoreDigest=()=>{delete window.releaseRestoreDigest;crypto.subtle.digest=original;resolve();};});}return out;};});await page.locator('#rndRestoreApply').click();await page.waitForFunction(()=>!!window.releaseRestoreDigest);await page.evaluate(()=>{const p=window.BringRndProject.current();p.title='Changed during manifest hash';window.dispatchEvent(new CustomEvent('rnd-project-saved',{detail:p}));window.releaseRestoreDigest();});await page.waitForFunction(()=>document.getElementById('portfolioStatus').textContent.includes('검토 중'));assert.equal(await page.locator('#projectPicker option[value="review-0"]').count(),0);await page.waitForFunction(()=>document.getElementById('archiveImport').value==='');console.log('PASS delayed restore manifest hash cannot apply after newer project change');
 await page.locator('#archiveImport').setInputFiles(largePayload);await page.locator('#rndRestoreReview').waitFor();await page.evaluate(()=>{const p=window.BringRndProject.current();p.title='Changed while review';window.dispatchEvent(new CustomEvent('rnd-project-saved',{detail:p}));});await page.locator('#rndRestoreApply').click();await page.waitForFunction(()=>document.getElementById('portfolioStatus').textContent.includes('검토 중'));assert.equal(await page.locator('#projectPicker option[value="review-0"]').count(),0);
 await page.locator('#archiveImport').setInputFiles(largePayload);await page.locator('#rndRestoreReview').waitFor();await page.evaluate(()=>window.dispatchEvent(new Event('rnd-session-reset')));await page.waitForFunction(()=>!document.getElementById('rndRestoreReview'));assert.equal(await page.evaluate(()=>window.BringRndProject.current()),null);assert.equal(await page.locator('#projectPicker option').count(),0);assert.equal(await page.locator('#rndRestoreManifestDownload').isDisabled(),true);assert.equal(await page.locator('#rndRestoreRollback').isDisabled(),true);assert.equal(await page.evaluate(()=>window.BringRndRollbackPending()),false);console.log('PASS native stale restore review rejects changed project and reset cancels application');
}finally{await app.evaluate(({app})=>app.exit(0)).catch(()=>{});await app.close().catch(()=>{});if(!resolve(exportDir).startsWith(resolve(tmpdir())+sep))throw Error('Unsafe temp cleanup');await rm(exportDir,{recursive:true,force:true});}
