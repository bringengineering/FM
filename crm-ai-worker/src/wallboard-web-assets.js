export const WALLBOARD_WEB_VERSION='tv-web-2026-09-20-1';

const html=`<!doctype html><html lang="ko"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>BRING 운영보드</title><link rel="stylesheet" href="/tv/app.css"></head><body><main><header class="top"><div class="brand"><span class="mark">BR</span><div><b>BRING ENGINEERING</b><small>회사 운영보드</small></div></div><div class="clock"><time id="clock">--:--</time><span id="connection">연결 확인 중</span></div></header><section id="pairing" class="pairing" hidden><p class="eyebrow">TV 최초 연결</p><h1>CRM에서 연결 코드를 승인해 주세요</h1><output id="pair-code">--------</output><p id="pair-expiry">승인 코드를 준비하고 있습니다.</p></section><section id="board" class="board" hidden><div class="scene-head"><div><p class="eyebrow">BRING 업무지시 · 성과관리</p><h1 id="scene-title">회사 현황</h1></div><nav><button id="previous" type="button" aria-label="이전 화면">‹</button><div id="dots" aria-label="화면 순서"></div><button id="next" type="button" aria-label="다음 화면">›</button></nav></div><div id="content" class="content"></div><footer><span id="data-state">게시 자료 확인 중</span><span id="position">1 / 5</span></footer></section></main><script src="/tv/app.js" defer></script></body></html>`;

const css=`:root{color-scheme:light;--blue:#2878ed;--blue-soft:#eaf3ff;--ink:#172536;--muted:#6c7d90;--line:#dfe8f1;--surface:#f4f7fa;--good:#16a36a;--warn:#f0a51a;--bad:#e45151;font-family:Inter,"Pretendard","Noto Sans KR",system-ui,sans-serif}*{box-sizing:border-box}body{margin:0;background:var(--surface);color:var(--ink);min-width:960px;min-height:100vh}main{min-height:100vh;padding:2.1vh 2.2vw;display:flex;flex-direction:column;gap:2vh}.top{height:9vh;display:flex;align-items:center;justify-content:space-between;background:#fff;border:1px solid var(--line);border-radius:22px;padding:0 1.7vw;box-shadow:0 12px 35px #17324b0d}.brand{display:flex;align-items:center;gap:15px}.mark{display:grid;place-items:center;width:52px;height:52px;border-radius:16px;background:linear-gradient(145deg,#2687f6,#0c4da3);color:white;font-weight:900;font-size:20px}.brand div{display:grid;gap:4px}.brand b{font-size:18px;letter-spacing:.04em}.brand small,.clock span{color:var(--muted);font-size:14px}.clock{text-align:right;display:grid;gap:3px}.clock time{font-size:29px;font-weight:800;font-variant-numeric:tabular-nums}.board,.pairing{flex:1;background:#fff;border:1px solid var(--line);border-radius:28px;padding:3vh 2.3vw;box-shadow:0 18px 45px #17324b10}.pairing{display:flex;flex-direction:column;align-items:center;justify-content:center;text-align:center}.pairing[hidden],.board[hidden]{display:none}.pairing h1{font-size:clamp(32px,3vw,54px);margin:10px}.pairing output{margin:3vh 0;padding:20px 34px;border-radius:20px;background:var(--blue-soft);color:#115fc9;font-size:clamp(56px,8vw,110px);font-weight:900;letter-spacing:.16em;font-variant-numeric:tabular-nums}.eyebrow{margin:0 0 8px;color:var(--blue);font-weight:800;letter-spacing:.04em}.scene-head{display:flex;justify-content:space-between;align-items:center;border-bottom:1px solid var(--line);padding-bottom:2vh}.scene-head h1{font-size:clamp(32px,3.2vw,58px);margin:0}.scene-head nav{display:flex;align-items:center;gap:12px}.scene-head button{width:48px;height:48px;border:1px solid var(--line);border-radius:50%;background:white;color:var(--ink);font-size:30px}.scene-head button:focus-visible{outline:4px solid #73a9ff}.scene-head #dots{display:flex;gap:8px}.dot{width:9px;height:9px;border-radius:50%;background:#cad5df}.dot.active{width:28px;border-radius:8px;background:var(--blue)}.content{min-height:62vh;padding:3vh 0;display:grid;align-content:start}.grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:1.4vw}.card{min-height:18vh;border:1px solid var(--line);border-radius:22px;padding:2.1vh 1.5vw;background:#fff;box-shadow:0 10px 26px #193b5c0b;display:flex;flex-direction:column;gap:10px}.card.accent{background:linear-gradient(150deg,#e9f3ff,#fff);border-color:#cfe3fc}.card b{font-size:clamp(18px,1.5vw,28px)}.card strong{font-size:clamp(42px,5vw,88px);line-height:1;font-variant-numeric:tabular-nums}.card span{color:var(--muted);font-size:clamp(14px,1.05vw,20px)}.progress{height:14px;border-radius:9px;background:#e8eef4;overflow:hidden;margin-top:auto}.progress i{display:block;height:100%;background:linear-gradient(90deg,#2a7ff0,#50a8ff);border-radius:inherit}.notice{display:grid;place-items:center;min-height:48vh;text-align:center;padding:4vh;background:linear-gradient(145deg,var(--blue-soft),#fff);border:1px solid #cfe3fc;border-radius:26px}.notice p{max-width:1000px;margin:0;font-size:clamp(32px,4vw,70px);font-weight:850;line-height:1.35;word-break:keep-all}.timeline{display:grid;gap:14px}.timeline .row{display:grid;grid-template-columns:150px 1fr 190px;gap:22px;align-items:center;padding:18px 24px;border:1px solid var(--line);border-radius:18px}.timeline time{font-size:28px;font-weight:800}.pill{justify-self:end;padding:8px 15px;border-radius:30px;background:var(--blue-soft);color:#1468d4;font-weight:750}.empty{display:grid;place-items:center;min-height:45vh;color:var(--muted);font-size:28px}.board footer{display:flex;justify-content:space-between;color:var(--muted);font-size:14px;border-top:1px solid var(--line);padding-top:1.5vh}@media(max-width:1200px){.grid{grid-template-columns:repeat(2,minmax(0,1fr))}.content{min-height:58vh}.timeline .row{grid-template-columns:110px 1fr 150px}}@media(max-height:800px){main{padding:1.2vh 1.5vw;gap:1vh}.top{height:8vh}.board,.pairing{padding:2vh 2vw}.content{min-height:58vh;padding:1.5vh 0}.card{min-height:15vh;padding:1.3vh 1.3vw;gap:5px}.card strong{font-size:clamp(34px,4vw,58px)}.timeline{gap:8px}.timeline .row{padding:10px 18px}.notice{min-height:42vh}.scene-head h1{font-size:36px}}`;

const js=`(()=>{'use strict';
const appVersion='${WALLBOARD_WEB_VERSION}';
const sceneKeys=['people','status','issues','notice','schedule'];
const labels={people:'팀 성과',status:'회사 현황',issues:'확인할 이슈',notice:'회사 공지',schedule:'오늘 일정'};
const cacheKey='bring-public-wallboard';let pendingToken='',board=null,index=0,page=0,elapsed=0,busy=false;
const byId=id=>document.getElementById(id),content=byId('content'),pairing=byId('pairing'),boardView=byId('board');
function node(tag,className,value){const element=document.createElement(tag);if(className)element.className=className;if(value!==undefined)element.textContent=String(value);return element;}
function valid(value){if(!value||typeof value!=='object'||!Number.isSafeInteger(value.version)||typeof value.notice!=='string'||typeof value.dataDate!=='string'||!Array.isArray(value.playlist)||!value.model||typeof value.model!=='object')return null;const counts=value.model.counts,schedule=value.model.schedule;if(!counts||typeof counts!=='object'||!Array.isArray(value.model.people)||!schedule||typeof schedule!=='object'||typeof schedule.available!=='boolean'||!Array.isArray(schedule.entries))return null;const numbers=[value.model.total,value.model.overdue,value.model.unknown,counts.assigned,counts.doing,counts.submitted,counts.returned,counts.done];if(numbers.some(number=>!Number.isSafeInteger(number)||number<0))return null;if(value.model.people.some(person=>!person||typeof person.name!=='string'||[person.total,person.done,person.overdue].some(number=>!Number.isSafeInteger(number)||number<0)))return null;if(schedule.entries.some(entry=>!entry||typeof entry.time!=='string'||typeof entry.status!=='string'))return null;if(value.playlist.some(item=>!item||!sceneKeys.includes(item.key)||typeof item.enabled!=='boolean'||!Number.isInteger(item.seconds)||item.seconds<10||item.seconds>120))return null;return value;}
function store(value){try{localStorage.setItem(cacheKey,JSON.stringify(value));}catch{}}
function restore(){try{return valid(JSON.parse(localStorage.getItem(cacheKey)||'null'));}catch{return null;}}
function clearCache(){try{localStorage.removeItem(cacheKey);}catch{}}
async function api(path,options={}){const response=await fetch('/tv/api/'+path,{credentials:'same-origin',cache:'no-store',...options,headers:{'content-type':'application/json',...(options.headers||{})}});const data=await response.json().catch(()=>({ok:false,code:'WALLBOARD_UNAVAILABLE'}));if(!response.ok)throw Object.assign(new Error(data.code||'WALLBOARD_UNAVAILABLE'),{code:data.code||'WALLBOARD_UNAVAILABLE'});return data;}
async function refreshApplication(){try{const response=await fetch('/tv/version',{cache:'no-store'});if(!response.ok)return;const data=await response.json();if(typeof data.version==='string'&&data.version!==appVersion){const url=new URL(location.href);url.searchParams.set('tvVersion',data.version);location.replace(url.toString());}}catch{}}
function card(title,value,caption,accent=false){const item=node('article','card'+(accent?' accent':''));item.append(node('b','',title),node('strong','',value),node('span','',caption));return item;}
function bar(item,done,total){const wrap=node('div','progress'),fill=node('i');fill.style.width=(total?Math.round(done/total*100):0)+'%';wrap.append(fill);item.append(wrap);}
function renderPeople(model){const grid=node('div','grid');for(const person of model.people.slice(page*6,page*6+6)){const item=card(person.name,person.done+' / '+person.total,'완료 · 지연 '+person.overdue+'건',person.overdue===0);bar(item,person.done,person.total);grid.append(item);}return grid.childElementCount?grid:node('p','empty','등록된 담당 업무가 없습니다.');}
function renderStatus(model){const grid=node('div','grid'),done=model.counts.done,total=model.total;const items=[['전체 업무',total,'현재 등록'],['완료',done,total?Math.round(done/total*100)+'% 진행률':'업무 없음'],['진행 중',model.counts.doing,'수행 중'],['검수 대기',model.counts.submitted,'결과 확인 필요'],['기한 지남',model.overdue,'우선 조치'],['상태 확인',model.unknown,'분류 필요']];items.forEach((value,i)=>grid.append(card(value[0],value[1],value[2],i<2)));return grid;}
function renderIssues(model){const grid=node('div','grid');grid.append(card('기한 지남',model.overdue,'완료되지 않은 지연 업무',model.overdue===0),card('반려·보완',model.counts.returned,'다시 제출할 업무',model.counts.returned===0),card('상태 미확인',model.unknown,'담당자 확인 필요',model.unknown===0));return grid;}
function renderNotice(value){const wrap=node('div','notice');wrap.append(node('p','',value||'등록된 회사 공지가 없습니다.'));return wrap;}
function renderSchedule(schedule){if(!schedule.available)return node('p','empty','오늘 일정 자료를 확인할 수 없습니다.');if(!schedule.entries.length)return node('p','empty','오늘 등록된 일정이 없습니다.');const list=node('div','timeline');for(const entry of schedule.entries.slice(page*6,page*6+6)){const row=node('div','row');row.append(node('time','',entry.time),node('b','','회사 일정'),node('span','pill',entry.status));list.append(row);}return list;}
function active(){const enabled=board?.playlist?.filter(item=>item.enabled&&sceneKeys.includes(item.key))||[];return enabled.length?enabled:sceneKeys.map(key=>({key,seconds:15}));}
function pages(scene){const count=scene.key==='people'?board.model.people.length:scene.key==='schedule'?board.model.schedule.entries.length:0;return Math.max(1,Math.ceil(count/6));}
function render(){if(!board){boardView.hidden=true;return;}pairing.hidden=true;boardView.hidden=false;const scenes=active();index=((index%scenes.length)+scenes.length)%scenes.length;const scene=scenes[index];page=Math.min(page,pages(scene)-1);byId('scene-title').textContent=labels[scene.key]||'운영보드';content.replaceChildren();const model=board.model;const view=scene.key==='people'?renderPeople(model):scene.key==='status'?renderStatus(model):scene.key==='issues'?renderIssues(model):scene.key==='notice'?renderNotice(board.notice):renderSchedule(model.schedule);content.append(view);const dots=byId('dots');dots.replaceChildren();scenes.forEach((_,position)=>dots.append(node('i','dot'+(position===index?' active':''))));byId('position').textContent=(index+1)+' / '+scenes.length+(pages(scene)>1?' · '+(page+1)+'페이지':'');byId('data-state').textContent='자료 기준 '+board.dataDate+' · 게시 버전 '+board.version+' · 웹 자동반영';}
function pairingView(code,expiresAt){boardView.hidden=true;pairing.hidden=false;byId('pair-code').textContent=code;byId('pair-expiry').textContent='CRM 회사 운영보드에서 승인 · 만료 '+new Date(expiresAt).toLocaleTimeString('ko-KR');}
async function begin(){const data=await api('pair/start',{method:'POST',body:'{}'});pendingToken=data.pendingToken;pairingView(data.code,data.expiresAt);byId('connection').textContent='승인 대기 중';}
async function poll(){if(!pendingToken)return;try{const data=await api('pair/poll',{method:'POST',body:JSON.stringify({pendingToken})});if(data.status==='approved'){pendingToken='';byId('connection').textContent='연결됨';await refresh();}}catch(error){if(error.code==='AUTH_REQUIRED'||error.code==='INVALID_TOKEN'){pendingToken='';await begin();}else byId('connection').textContent='승인 서버 연결 확인 중';}}
async function refresh(){if(busy)return;busy=true;try{const data=await api('display');const next=valid(data.board);if(data.board!==null&&!next)throw Object.assign(new Error('INVALID_PUBLICATION'),{code:'INVALID_PUBLICATION'});if(next){const previous=board;board=next;try{render();store(next);}catch(error){board=previous;render();throw error;}}byId('connection').textContent=next?'서버 연결됨':'승인됨 · 게시 대기';}catch(error){if(error.code==='AUTH_REQUIRED'||error.code==='INVALID_TOKEN'){pendingToken='';board=null;clearCache();await begin();}else{board=board||restore();byId('connection').textContent='연결 확인 중 · 최근 게시자료 유지';render();}}finally{busy=false;}}
function move(step){const scenes=active(),scene=scenes[index];if(step>0&&page+1<pages(scene))page++;else if(step<0&&page>0)page--;else{index=(index+step+scenes.length)%scenes.length;page=step<0?pages(scenes[index])-1:0;}elapsed=0;render();}
byId('previous').addEventListener('click',()=>move(-1));byId('next').addEventListener('click',()=>move(1));
setInterval(()=>{byId('clock').textContent=new Date().toLocaleTimeString('ko-KR',{hour:'2-digit',minute:'2-digit'});if(document.visibilityState==='visible'&&board){const seconds=active()[index]?.seconds||15;if(++elapsed>=seconds)move(1);}},1000);
setInterval(()=>{if(pendingToken)void poll();},5000);setInterval(()=>{if(!pendingToken)void refresh();},15000);setInterval(()=>void refreshApplication(),60000);
board=restore();render();void refresh();
})();`;

const securityHeaders={
 'content-security-policy':"default-src 'none'; script-src 'self'; style-src 'self'; connect-src 'self'; img-src 'self' data:; font-src 'self'; base-uri 'none'; form-action 'none'; frame-ancestors 'none'; object-src 'none'",
 'x-content-type-options':'nosniff','x-frame-options':'DENY','referrer-policy':'no-referrer','permissions-policy':'camera=(), microphone=(), geolocation=(), payment=()'
};
export function wallboardWebAssetResponse(path){
 if(path==='/tv/version')return new Response(JSON.stringify({version:WALLBOARD_WEB_VERSION}),{headers:{...securityHeaders,'content-type':'application/json; charset=utf-8','cache-control':'no-store'}});
 const assets={
  '/tv':{body:html,type:'text/html; charset=utf-8'},'/tv/':{body:html,type:'text/html; charset=utf-8'},
  '/tv/app.css':{body:css,type:'text/css; charset=utf-8'},'/tv/app.js':{body:js,type:'text/javascript; charset=utf-8'}
 };
 const asset=assets[path];
 if(!asset)return new Response(JSON.stringify({ok:false,code:'NOT_FOUND'}),{status:404,headers:{'content-type':'application/json; charset=utf-8','cache-control':'no-store','x-content-type-options':'nosniff'}});
 return new Response(asset.body,{headers:{...securityHeaders,'content-type':asset.type,'cache-control':'no-store'}});
}
